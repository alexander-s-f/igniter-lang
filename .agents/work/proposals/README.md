# Igniter-Lang Proposals Index

Status: Stage 3 active intake
Maintainer: `[Igniter-Lang Compiler/Grammar Expert]`
Stage 1 closed: 2026-05-06 (META-EXPERT-007)
Stage 2 closed: 2026-05-07 (META-EXPERT-009.1)

---

## Accepted — Stage 1 Frozen

All Stage 1 PROPs are frozen and read-only.

→ See [proposals/accepted/](accepted/README.md) — 20 files.

Do not modify accepted PROPs. Errata may be added alongside originals.

---

## Stage 2 — Closed (2026-05-07)

Stage 2 PROPs are closed when their experiment reached PASS verdict in META-EXPERT-009.1.
Closed PROPs remain in `proposals/` for reference. They are not moved to `accepted/`
(that directory is Stage 1 only).

| File | Status | Summary |
|------|--------|---------|
| [PROP-022](PROP-022-history-type-constructor-v0.md) | accepted | History[T]/BiHistory[T]; temporal operations; Stage 2 experiment PASS (META-EXPERT-009.1); Stage 3 extensions: PROP-028, PROP-022A |
| [PROP-023](PROP-023-stream-input-surface-v0.md) | accepted | stream T; window; fold_stream; KPN grounding; Stage 2 experiment PASS (META-EXPERT-009.1) |
| [PROP-024](PROP-024-olap-point-primitive-v0.md) | accepted | OLAPPoint[T,Dims]; olap_point declaration; Stage 2 experiment PASS (META-EXPERT-009.1) |
| [PROP-025](PROP-025-invariant-severity-levels-v0.md) | accepted | invariant severity :error/:warn/:soft/:metric; Stage 2 experiment PASS (META-EXPERT-009.1); OOF-I1/I3/I5 deferred |
| [PROP-026](PROP-026-parser-oof-hardening-spec-v0.md) | accepted | Parser OOF hardening; PH-1..PH-8, PF-1..PF-4; Stage 2 experiment PASS (META-EXPERT-009.1) |
| [PROP-027](PROP-027-production-compiler-diagnostics-contract-v0.md) | accepted | Production compiler CLI + diagnostics contract; CL-1..CL-10; Stage 2 experiment PASS (META-EXPERT-009.1) |

---

## Stage 3 — Active

| File | Status | Summary |
|------|--------|---------|
| [LANG-STDLIB-COLLECTION-FIRST-LAST-P2](LANG-STDLIB-COLLECTION-FIRST-LAST-P2-ruby-parity-proof-v0.md) | closed / proved — 62/62 PASS | Ruby parity for `stdlib.collection.first` / `stdlib.collection.last`: `first(Collection[T])` and `last(Collection[T])` now compile in Ruby as `Option[T]`, with canonical Ruby SIR calls `stdlib.collection.first` / `stdlib.collection.last`; `or_else(first(xs), default)` is the only ergonomic dual-clean reader until sealed Option matchability lands. Implementation is bounded to Ruby TC dispatch/helper plus inventory entries and digest; Rust lab remains unchanged and is recorded as existing evidence only. Intentional Rust-parity fallback: malformed/non-inferable inputs such as `first()` or `first(String)` return `Option[Unknown]` without new `OOF-COL*` diagnostics. CLOSED: no Option/Result matchability, no `some`/`none`, no parser/emitter/runtime/app migration, no sort/order_by/query API. proof: `experiments/stdlib_collection_first_last_option_proof/verify_stdlib_collection_first_last_p2.rb`; doc/card: `.agents/work/proposals/LANG-STDLIB-COLLECTION-FIRST-LAST-P2-ruby-parity-proof-v0.md` / `.agents/work/cards/lang/LANG-STDLIB-COLLECTION-FIRST-LAST-P2.md`; P1 runner updated to fixed-state |
| [LANG-STDLIB-RESULT-BIND-P2](LANG-STDLIB-RESULT-BIND-P2-implementation-planning-v0.md) | closed / proved — 66/66 PASS | Impl planning (Candidate A): complete built-in `Result[T,E]` monad for lead_router LR-P01. SURFACE = **`stdlib.result.{ok, err, unwrap_or, and_then}`** (map DEFERRED — `map` owned by `stdlib.collection.map`; `map(Result,…)`→OOF-COL2 both). `and_then` = `Result[T,E] × (T -> Result[U,E]) -> Result[U,E]`, **E fixed, LAMBDA-ONLY**, static contract callable inside lambda, **no dynamic callee**. STATE (live): Ruby = NONE (all 4 → Unknown function); Rust = PARTIAL (`ok`@647/`err`@662/`and_then`@558 arms exist but infer Unknown payload → OOF-TY1; `unwrap_or`@327 clean) ⇒ work = **Ruby parity + dual payload inference** (same sites + same `output_type_hints` lever as Sumtype P3). NAMESPACE `stdlib.result.*` distinct from `stdlib.outcome.*` (PROP-047 Map predicates) + `collection.map`. SIR coordinated with Sumtype P2: ok/err → `variant_construct`+`sealed:true` (label value/error); and_then/unwrap_or → ordinary stdlib `call` nodes (no new node). Ruby anchors = new arms before `Unknown function` fallthrough (`typechecker.rb`~:1055). DIAGNOSTICS reuse OOF-TY0/TY1 + COL-style first-arg check (no new code). KEY: **bind needs CONSTRUCTION, not source `match`** (and_then+unwrap_or consume Result functionally) ⇒ ROUTE = thin Result layer **gated on Sumtype P3** (ok/err sealed construction+SIR); RECOMMENDATION = **fold the 4 Result arms into Sumtype P3** (identical sites+lever) or land immediately after. APP PROOF: lead_router's **7** `match prev {Reject=>carry;Proceed=>work}` steps → one `and_then` chain over `Result[Ctx,Reject]`, no user-variant, no dynamic dispatch. CLOSED: no parser/TC/emitter/runtime change, no app migration, no user-variant generic bind (Cand B rejected), no retry/policy/capability/receipt, no dynamic-dispatch widening. proof: `experiments/stdlib_result_bind_proof/verify_stdlib_result_bind_p2.rb`; doc: `LANG-STDLIB-RESULT-BIND-P2-implementation-planning-v0.md`; card: `.agents/work/cards/lang/LANG-STDLIB-RESULT-BIND-P2.md`; next: fold into SUMTYPE-P3 or RESULT-BIND-P3 → lead_router migration |
| [LANG-SUMTYPE-CONSTRUCT-MATCH-P3](LANG-SUMTYPE-CONSTRUCT-MATCH-P3-implementation-proof-v0.md) | closed / implemented — 109/109 PASS | **Dual-toolchain implementation** of sealed built-in `Option[T]`/`Result[T,E]` construct + source `match` (canon Ruby + lab Rust, in lockstep). LANDED: `some/none/ok/err` → sealed `variant_construct` SIR (`sealed:true`), reusing user-variant construct/emit machinery (payload labels locked **value/error**); `match` over Option/Result admitted via a **separate** `SEALED_VARIANT_SHAPES` registry (NOT merged into `@variant_shapes`, so emitted `variant_env` is unchanged) — **admission == registration** ⇒ OOF-KIND4 relaxes for sealed only, arbitrary types stay rejected; match payload bindings substituted to the **concrete** subject param (`Some.value:T`/`Ok.value:T`/`Err.error:E`); expected-type propagation (`sealed_output_hints`, parallel to record-literal `output_type_hints`) recovers the missing param for `none()/ok()/err()` at an Option/Result-annotated boundary; `unwrap_or` (Result/Option reader) + `and_then` (fixed-error-family bind, lambda arg, static `call_contract` allowed, no dynamic callee) dual; `first`/`last` matchable as `Option[T]`. SIR byte-parity holds (both canonicalise by sorting keys; `sealed` marker sealed-ONLY ⇒ user-variant SIR byte-unchanged modulo pre-existing volatile-key normalization). REFINED vs P2 plan: sealed registry held SEPARATELY (avoids `variant_env` SIR change); constructors lower to `variant_construct` (canon P1/P2 decision) superseding the Rust lab `call`-node approximation — no dual-clean app contained these before (Ruby rejected them) so no golden regressed. REGRESSIONS: P1 76/76 + P2 78/78 (updated to fixed-state), prop044 75/75, optional-field 57/57, compose-entity 71/71, **fleet 19/20 DUAL-CLEAN** (rule_engine frozen boundary unchanged, touches no P3 surface). INVENTORY unchanged (constructors are sealed constructors not call-surface; `unwrap_or`/`and_then` emit bare SIR — canonical-qualified promotion deferred to a dedicated migration to preserve entry-contract invariants). CLOSED: no generic Monad/typeclass/do-notation, no constructor-form `Some{…}` (still OOF-KIND2), no dynamic-dispatch widening, no nullable runtime, no app migration. proof: `experiments/sumtype_construct_match_proof/verify_sumtype_construct_match_p3.rb`; doc: `LANG-SUMTYPE-CONSTRUCT-MATCH-P3-implementation-proof-v0.md`; card: `.agents/work/cards/lang/LANG-SUMTYPE-CONSTRUCT-MATCH-P3.md`; next: RESULT-BIND-P2 follow-on (lead_router LR-P01) / OPTIONAL-FIELD-P3 / inventory-promotion / stale-readiness-proof sweep |
| [LANG-SUMTYPE-CONSTRUCT-MATCH-P2](LANG-SUMTYPE-CONSTRUCT-MATCH-P2-implementation-planning-v0.md) | closed / proved — 78/78 PASS | Implementation planning for sealed built-in `Option[T]`/`Result[T,E]` construct + source `match`. KEY: the two toolchains are **structurally symmetric** (3-level `variant_shapes` registry + byte-identical `OOF-KIND4` "not a variant" guard in `infer_match_expr` + same `variant_construct`/`match_node` SIR) ⇒ admission = **5 MIRRORED edit sites/side**: (1) register sealed Option/Result into `variant_shapes` at TC startup [Ruby `variant_type?`:238 / Rust `build_variant_shapes`:3555+init:300-303]; (2) relax OOF-KIND4 for `{Option,Result}` only [Ruby `infer_match_expr`:3194-3201 / Rust :3732-3745]; (3) construct via `some/none/ok/err` [Ruby call-dispatch Unknown-fn fallthrough:1055 / Rust `typechecker/stdlib_calls.rs` some@628/none@640/ok@647/err@662]; (4) `sealed:true` SIR marker on Option/Result nodes ONLY (user-variant byte-unchanged) [Ruby emitter:442/453 / Rust annotated:3695-3701+emitter match_node rename:887-891]; (5) refinement: expected-type propagation for `none/ok/err` partial payload. LOCKED: registry `Option{Some{value:T}|None{}}`+`Result{Ok{value:T}|Err{error:E}}` (payload labels **value/error** — resolves agent divergence on Err); construct=function aliases (constructor-form `Some{…}`→OOF-KIND2, deferred); match arms `Some{value}`/`None`/`Ok{value}`/`Err{error}`; diagnostics reuse OOF-KIND1/2/4+TY0/1 (no new code); TC owns it (parser untouched). SUB-GAP confirmed live: `none()`→Option[Unknown]→OOF-TY1 vs Option[Integer] (some/first resolve; fix=`output_type_hints` lever, separable P3b). ROUTE: **single dual-toolchain P3** — SIR byte-parity is the top risk (Ruby/Rust split would diverge the 16-app SIR goldens). REGRESSIONS: P1 76/76, P2 78/78, optional-field 57/57, first/last 54/54, outcome-bind 56/56, user-variant SIR byte-identical, 16-app fleet + SIR goldens, rule_engine fail-closed. CLOSED: no parser/TC/emitter/runtime change, no app migration, no nullable runtime, no dynamic-dispatch widening, no impl. proof: `experiments/sumtype_construct_match_proof/verify_sumtype_construct_match_p2.rb`; doc: `LANG-SUMTYPE-CONSTRUCT-MATCH-P2-implementation-planning-v0.md`; card: `.agents/work/cards/lang/LANG-SUMTYPE-CONSTRUCT-MATCH-P2.md`; next: LANG-SUMTYPE-CONSTRUCT-MATCH-P3 (single dual-toolchain impl) → FIRST-LAST-P2 / RESULT-BIND-P2 / OPTIONAL-FIELD-P3 |
| [LANG-SUMTYPE-CONSTRUCT-MATCH-P1](LANG-SUMTYPE-CONSTRUCT-MATCH-P1-sumtype-v0.md) | closed / proved — 76/76 PASS | **Unifies** the 3 converging Option/Result debt tracks (first/last→Option, outcome/Result bind, optional-field `T?≡Option`) into one readiness picture + v0 route for built-in sum-type **construction + source `match`**. MEASURED dual-toolchain: `Option[T]`/`Result[T,E]` are recognized **types** in both; **`or_else` is the ONLY dual-clean reader**; CONSTRUCTION diverges — Ruby NONE (`some/none/ok/err/unwrap_or/and_then/first` → `OOF-TY0 Unknown function`), Rust PARTIAL (`some`/`unwrap_or`/`first` clean; `none`/`ok`/`err`/`and_then` recognized but payload `Unknown`→`OOF-TY1`); MATCH dual-broken **consistently** — `match opt {Some/None}` → **`OOF-KIND4`** "subject 'Option' not a variant" (PROP-044 §9.1 deferral) in BOTH; constructor-form `Some{…}` → `OOF-KIND2` both; **user-`variant` match IS dual-clean** → the `variant_construct`/`match_node` machinery exists, only the OOF-KIND4 non-variant guard blocks Option/Result. ROUTE: recognize Option/Result as **SEALED BUILT-IN VARIANTS reusing the variant match path** (lift OOF-KIND4 for the 2 sealed types); v0 construct = **function aliases** `some/none/ok/err` (ch8-spec'd, Rust-partial; constructor-form collides → OOF-KIND2), v0 match = variant-style arms `Some/None/Ok/Err`; SIR = **sealed marker on `variant_construct`/`match_node`** (not dedicated nodes); reuse OOF-KIND/OOF-TY (no new code). KEY ANSWERS: first/last Ruby parity **can land first** (or_else-only caveat); built-in **`Result.and_then` ⇒ satisfies lead_router LR-P01** (Candidate A: complete built-in Result, over B user-variant combinator); `T?` consumes via omit→`none()`/present→`some(T)`, **no nullable runtime**; ch8 specs some/none/ok/err/map/unwrap_or but **NOT `and_then`**; inventory has only `option.or_else` (+`wrap` orphan). → **P2 IMPLEMENTATION PLANNING** (real ambiguity: construct/match spelling asymmetry + SIR marker shape). CLOSED: no parser/TC/emitter/runtime change, no optional-field/first-last/and_then impl, no nullable runtime, no dynamic-dispatch widening, no app migration. proof: `experiments/sumtype_construct_match_proof/verify_sumtype_construct_match_p1.rb`; doc: `LANG-SUMTYPE-CONSTRUCT-MATCH-P1-sumtype-v0.md`; card: `.agents/work/cards/lang/LANG-SUMTYPE-CONSTRUCT-MATCH-P1.md`; next: LANG-SUMTYPE-CONSTRUCT-MATCH-P2 → FIRST-LAST-P2 / RESULT-BIND-P2 / OPTIONAL-FIELD-P3 |
| [LANG-STDLIB-COLLECTION-FIRST-LAST-OPTION-P1](LANG-STDLIB-COLLECTION-FIRST-LAST-OPTION-P1-v0.md) | closed / proved — 54/54 PASS | Readiness for call_router CR-P03: picking the most-recent matched candidate (`candidates.order(desc).first`) wants `first`/`last` → **matchable `Option[T]`**. TWO SEPARABLE GAPS: (1) **first/last NOT dual-clean** — Ruby TC `Unknown function: first`/`last`; Rust TC implements them → **`Option[T]`** (`first(Collection[Integer])`:`Option[Integer]` ok; `last` parity); (2) **`Option[T]` NOT source-matchable** — Rust `match opt { Some{…} None{…} }` → **`OOF-KIND4`** ("subject … not a variant"; deferred PROP-044 §9.1) — user variants ARE matchable (app's `CustomerPhoneOf` matches `variant Telephony`). App sidesteps both: `matched_call` is INJECTED, pure `MatchCall` only `count`s hits → user `MatchResult` variant. or_else INSUFFICIENT (no default `CallrailCall` for "most recent call" → matchability required, not or_else). Q5: first/last should return **`Option[T]`** (total; empty→None), not Unknown/fail. Q6: NO sort/order_by — ordering is DB/injected, `.first` pressure suffices (scope stays narrow). ROUTE: **SPLIT TRACK (P2 planning)** — Track 1 = dual-toolchain first/last parity (Ruby gains them → Option[T]; cheap, mechanical, **insufficient alone**); Track 2 = **Option construction + matchability** (lift PROP-044 §9.1) = the real unblock, **SHARED** with LANG-OPTIONAL-FIELD-PARTIAL-RECORD-PROP-P2 (`T?`≡Option) + LANG-STDLIB-OUTCOME-BIND-P1 (`Result[T,E]`) → the built-in **sum-type construction+matchability debt** (3rd card to converge; recommend a unified sum-type track). No implementation P1. CLOSED: no impl, no query/order_by/sort, no DB `.first` authority, no Option runtime change, no optional-field impl, no app migration. proof: `experiments/stdlib_collection_first_last_option_proof/verify_stdlib_collection_first_last_option_p1.rb`; doc: `LANG-STDLIB-COLLECTION-FIRST-LAST-OPTION-P1-v0.md`; card: `.agents/work/cards/lang/LANG-STDLIB-COLLECTION-FIRST-LAST-OPTION-P1.md`; next: Track1 LANG-STDLIB-COLLECTION-FIRST-LAST-P2 + Track2 unified Option/Result matchability |
| [LANG-STDLIB-OUTCOME-BIND-P1](LANG-STDLIB-OUTCOME-BIND-P1-readiness-v0.md) | closed / proved — 56/56 PASS | Readiness for lead_router LR-P01: a dry-monads `Result`/`.bind` railway re-implemented BY HAND over a user `variant Pipe { Proceed{ctx} \| Reject{stage,message} }`. SOURCE-QUANTIFIED: **7** railway steps in `pipeline.ig` (`input prev : Pipe`→`output r : Pipe`), each re-writing the **identical** `Reject{stage,message} => Reject{…}` carry arm (FindTrade·FindVendor·FindZip·BusinessHours·ResolveMode·CheckAvailability·GenerateResults) + Validate (entry) + 2 terminal `match p` in service.ig — registry's "8 steps" over-counts by 1 (includes Validate, no carry). AXIS SEPARATION: existing **`stdlib.outcome.*`** (PROP-P3 60/60) = `Map[String,String]` **inspection** predicates (kind + PROP-047 is_denied/is_timed_out/…), different axis, **no bind/and_then** → does NOT address LR-P01; canon **`Result[T,E]`** spec'd (ch3 §3.1 Ok\|Err; ch8 §8.4 ok/err/map/unwrap_or) but **`and_then` absent from spec** AND **not dual-clean** (Ruby TC: Unknown function for ok/err/and_then/unwrap_or; Rust TC: has `"ok"`/`"err"`/`"and_then"` arms) — which is why the app uses a dual-clean user `variant`, not Result; LR-P01 = monadic **sequencing/short-circuit**, the missing primitive. Q2: short-circuit ≡ `.bind`; richer audit (`StepReceipt`) = separate LR-P02 fold-to-struct. Q5: combinator dispatches to **statically-named** steps (no variable callee) → LAB-DYNAMIC-CONTRACT-DISPATCH-P2 preserved. Q6: canonical alias `stdlib.outcome.and_then` (≡ dry-monads `.bind`) or complete `Result.and_then`. ROUTE: **P2 planning** — Candidate A (recommended) complete+unify canon `Result[T,E]` monad dual-clean (Ruby parity + `and_then`); Candidate B (fallback) `bind`/`and_then` combinator over a user 2-arm variant. NOT a convention, NOT 'no helper', no implementation here. CLOSED: no parser/TC/emitter/runtime, no generic sealed Outcome, no retry/policy, no VM authority, no app migration, dynamic dispatch stays closed. proof: `experiments/stdlib_outcome_bind_proof/verify_stdlib_outcome_bind_p1.rb`; doc: `LANG-STDLIB-OUTCOME-BIND-P1-readiness-v0.md`; card: `.agents/work/cards/lang/LANG-STDLIB-OUTCOME-BIND-P1.md`; next: LANG-STDLIB-OUTCOME-BIND-P2 (planning) |
| [LANG-STDLIB-STRING-CONTAINS-ENDS-WITH-P1](LANG-STDLIB-STRING-CONTAINS-ENDS-WITH-P1-v0.md) | closed / proved — 50/50 PASS | Readiness for call_router CR-P02 (phone suffix / `LIKE %suffix%` matching). PIVOT: the card's premise is **stale** — `contains`/`ends_with`/`starts_with` ALREADY EXIST as **`stdlib.text.*`**, **lifecycle=production-implemented, lowering=dual-toolchain**, return **Bool**, and accept **String** via the canon String→Text v0 compat rule. call_router looked under the wrong namespace (`stdlib.string`); predicates live under `stdlib.text`. VERIFIED dual-clean today: `ends_with(phone,suffix)`/`contains(...)`/`starts_with(...)` on String → Ruby 0 / Rust ok 0 (qualified + unqualified), usable directly as a `filter` predicate; call_router whole-app ok/0. CR-P02 source: `correlate.ig` MatchCall uses exact `c.customer_phone == customer_phone`; report cites production `customer_phone_number LIKE %suffix%` (real need). Diagnostics: OOF-TY0 (arity `expected 2 argument(s)` + type `arg 2: expected Text, got Integer`); no new code. char_at/substring (stdlib.string, lab-implemented) = precedent, not re-opened. ROUTE: **NO new implementation** — P2 implements ZERO functions (both already exist: ends_with=last-N, contains=%suffix%); CR-P02 unblockable today by swapping `==`→`ends_with`/`contains` (app-side, separate). Only optional residual: namespace consistency (text predicates vs string slicers) → CLOSED LANG-STRING-TEXT-ALIAS lineage. CLOSED: no impl, no regex, no locale/case-fold/Unicode, no fuzzy scoring, no phone parser, no DB LIKE authority, no app migration, no compiler/inventory edits. proof: `experiments/stdlib_string_surface_proof/verify_stdlib_string_contains_ends_with_p1.rb`; doc: `LANG-STDLIB-STRING-CONTAINS-ENDS-WITH-P1-v0.md`; card: `.agents/work/cards/lang/LANG-STDLIB-STRING-CONTAINS-ENDS-WITH-P1.md`; next: none required |
| [LANG-OPTIONAL-FIELD-PARTIAL-RECORD-PROP-P2](LANG-OPTIONAL-FIELD-PARTIAL-RECORD-PROP-P2-v0.md) | closed / proved — 57/57 PASS | Full PROP for optional record fields `field : T?`. DECISION: `T?` ≡ `Option[T]` (canon ch3 §3.1 `Option[T] -- Some(T) | None`, Stage-1). Omit → **None** (compiler-injected, record stays rectangular); present `v:T` → **Some(v)** (auto-wrap); present `Option[T]` → as-is. Read `r.field : Option[T]` → unwrap via existing **`or_else`** (PROP-043; fail-closed, no raw-T read, no null-deref). Runtime value is the **named** canon Option `None` arm — NOT untyped null. Output assignability: required strict (OOF-TY0 if missing), declared-optional may be absent (→None); aligned with canon **record width subtyping** `{a,b,c}<:{a,b}`; no relaxation beyond declared-optional. Wrong-type-present / unexpected / required-missing → OOF-TY0 **preserved** (fail-closed); no new OOF code. SIR rectangular + new `option_construct{some|none}` node (modeled on variant_construct; Option runtime repr already exists via map_get/or_else). v0 scope = construction + read-type; OUT: Some/None literal surface + Option match (deferred PROP-044 §9.1; or_else is v0 reader) + defaulted-fields (`T = default`, distinct feature) + app migration. GROUNDING (verified dual-toolchain): read path `Option[String]`+or_else dual-clean today; `?` is a dual-toolchain no-op (Ruby lexer drops; Rust keeps FieldDecl.optional but build_type_shapes drops); explicit Option fields currently unconstructable in literals → rules purely additive. ROUTE: **P3 implementation readiness — CONDITIONAL** on (1) `option_construct` SIR node both emitters, (2) coordinated read-site migration for `T?` apps (vector_editor GraphicObject path_pts/rect_data/text_data). INSERTION POINTS: Ruby type_shapes + infer_record_literal (missing-field OOF) + lexer `?` token; Rust build_type_shapes + check_record_literal_shape (required-missing OOF); Rust parser already captures optional. CLOSED: no impl, no app migration, no unnamed nullable, no output-assignability relaxation beyond optional rule, no Some/None surface, no defaulted-fields. proof: `experiments/optional_field_proof/verify_optional_field_prop_p2.rb`; doc: `LANG-OPTIONAL-FIELD-PARTIAL-RECORD-PROP-P2-v0.md`; card: `.agents/work/cards/lang/LANG-OPTIONAL-FIELD-PARTIAL-RECORD-PROP-P2.md`; next: LANG-OPTIONAL-FIELD-PARTIAL-RECORD-P3 (dual-toolchain impl, proof ≥60) |
| [LANG-STDLIB-MATH-P1](LANG-STDLIB-MATH-P1-readiness-v0.md) | closed / proposed — 47/47 PASS | Bounded integer standard library math surface (abs, sqrt, hypot) design proposal. Standardizes `abs(Integer)->Integer`, `sqrt(Integer)->Integer` (floor rounding, runtime 0-clamp, compile-time literal negative check), and `hypot(Integer,Integer)->Integer`. Proposes `OOF-MTH*` / `OOF-MTH1` namespace. Fixed-point scaling remains strictly app-level. No float/trig math. VM/runtime semantics are stdlib-only bindings. proof: `experiments/stdlib_math_proof/verify_stdlib_math_p1.rb`; card: `.agents/work/cards/lang/LANG-STDLIB-MATH-P1.md` |
| [LANG-FOLD-STRUCT-ACCUMULATOR-P4](LANG-FOLD-STRUCT-ACCUMULATOR-P4-lowering-parity-proof-v0.md) | closed / implemented — 83/83 PASS | Ruby SemanticIR lowering parity for ordinary `fold(...)`. Canon changes: Ruby TC preserves the typed fold lambda as emitter metadata; Ruby emitter lowers `fold` / `stdlib.collection.fold` to `map_reduce_aggregate` with structured pipeline stage `{kind:"fold", param_acc, param_val, init, body}`. Lab parity change: Rust emitter delegates ordinary `fold` compute expressions to the existing map/reduce optimizer and returns the structured stage for single-step fold, resolving the P3 artifact note without Rust TC work. Proof covers Ruby/Rust scalar and record fold SIR envelope parity, generic fold-call absence in emitted SIR, Ruby fold_stream fixture, map/count regression, and P3 runner still green. CLOSED: no parser changes, no Rust TC, no app migrations, no group_by/join/flat_map, no `-> { ... }` ergonomics, no runtime/IO/capability authority. proof: `experiments/fold_struct_accumulator_proof/verify_fold_struct_accumulator_p4.rb`; card: `.agents/work/cards/lang/LANG-FOLD-STRUCT-ACCUMULATOR-P4.md` |
| [LANG-FOLD-STRUCT-ACCUMULATOR-P3](LANG-FOLD-STRUCT-ACCUMULATOR-P3-rust-tc-proof-v0.md) | closed / implemented — 83/83 PASS | Bounded Rust TypeChecker implementation for record/struct accumulators in `fold(Collection[T], Acc, (Acc,T)->Acc)`. Implemented only in `igniter-lab/igniter-compiler/src/typechecker.rs`: compute-phase contextual fold upgrade from output/compute annotation named-record hints; `infer_fold_call_type`; `check_record_literal_shape_col4`; `"fold" =>` arm delegates to helper. Positive: scalar fold preserved, named seed, inline record seed from output, inline record seed from compute annotation, `call_contract` body returning Acc, app-pressure fixtures from trade_robot / air_combat / sim_framework, air_combat baseline clean. Negative: bad/missing/extra record fields, scalar/non-Acc body, wrong arity, wrong lambda params, non-Collection first arg, non-lambda third arg all route to `OOF-COL4`. Regressions: map/filter/count, sum/avg, append, scalar fold, fold_stream source guard. P2 proof updated to fixed-state assertions and passes 64/64. IMPORTANT P4 NOTE: Rust emitter source has a structured fold helper, but ordinary single-step fold artifacts still lower through existing generic call/apply; P3 leaves Rust emitter unchanged and routes artifact/SIR parity to P4. CLOSED: no parser, no Ruby, no Rust emitter, no app migration, no group_by/join/flat_map, no IO/runtime/capability, no new OOF code. proof: `igniter-lab/igniter-compiler/verify_fold_struct_accumulator_p3.rb`; card: `.agents/work/cards/lang/LANG-FOLD-STRUCT-ACCUMULATOR-P3.md`; next: LANG-FOLD-STRUCT-ACCUMULATOR-P4 lowering parity |
| [LANG-COMPOSE-ENTITY-PROP-P2](LANG-COMPOSE-ENTITY-PROP-P2-entity-prop-v0.md) | closed / full PROP authored — 71/71 PASS | Full same-module `entity` v0 PROP. Gates satisfied: P1 49/49, dynamic dispatch P2 47/47 fail-closed, fold-struct P2 64/64 non-blocking for core entity. Defines grammar (`entity` / one `state` / configs / static entity-level `uses` / actions), generated contract lowering, copy-on-write state threading, modifier/fragment inference via PROP-031, closed variant route via PROP-044, and `OOF-ENT1` through `OOF-ENT9`. `composes` remains reserved by PROP-016 and is not reused. Behavior calls are source identifiers checked against the entity `uses` set; generated executable v0 target uses literal `call_contract("Behavior", ...)` only after static validation, so no variable callee/runtime reflection/plugin lookup is introduced and current Rust contract-level `uses` parser support is not required. Fold collection-run syntax is deferred to a later extension after fold-to-record P3/P4. CLOSED: no parser/compiler/runtime implementation, no mutable runtime object, no IO/capability/effect authority, no inheritance, no cross-module entity behavior resolution, no app migration. proof: `experiments/compose_entity_proof/verify_compose_entity_prop_p2.rb`; card: `.agents/work/cards/lang/LANG-COMPOSE-ENTITY-PROP-P2.md`; next: LANG-COMPOSE-ENTITY-P3 implementation planning |
| [LANG-FOLD-STRUCT-ACCUMULATOR-P2](LANG-FOLD-STRUCT-ACCUMULATOR-P2-implementation-planning-v0.md) | closed / proved — 64/64 PASS | Implementation planning for record/struct accumulators in `fold`. ROUTE: **P3 (Rust TC lift) → P4 (lowering parity)** — two-card ladder, justified single-card fallback noted. GROUND TRUTH (live): record `Acc` is **not banned** — Ruby TC types it with `-> ({...})` (bad field → OOF-TY0; scalar clean); the gap is a cluster — **Rust TC** thin `"fold" =>` arm (`typechecker.rs:3664-3671`, `resolved_type = typed_args[1]` — no seed ctx, no return validation, no OOF-COL4) + inline record seed → Unknown (`output_type_hints`; "uncontextualized RecordLiterals remain Unknown" `:1130`) → OOF-TY1; **Ruby emitter** has NO fold node (generic `call`) while **Rust emitter** already emits structured `{kind:"fold", param_acc, param_val, init, body}` (`emitter.rs:1508-1540`, record-capable, no change). CANONICAL SIR = Rust fold node. v0 spelling `-> ({ ... })` (parser `-> { ... }` deferred, separate card). PLAN: **P3 Rust TC** = rewrite `"fold"` arm (arity/collection/bind acc·elem/infer body/`structurally_assignable`+`type_shapes` field check → OOF-COL4) + extend hint pass (`:677-740`/`:1130-1134`) to contextualize seed+body record literals from the compute node's output hint — REUSE not widen; no new OOF code. **P4 lowering parity** = Ruby emitter `fold_node` beside `fold_stream_node` + dispatch in `semantic_expr`(341) like the `recur` special-case; optional Ruby TC name→structural body-vs-Acc hardening (`:2718-2724`). APP PRESSURE: trade_robot RunBacktest p0..p10 unroll → fold; RSI/MACD `{sum_gain,sum_loss,prev_close,count}` (indicators.ig "fold() returns a single scalar"); sim_framework secondary. PROOF MATRIX (Q9): positive/negative(OOF-COL4 field cases)/app-pressure/regression(scalar+map/filter/sum/avg+fold_stream+cross-toolchain SIR diff); P3≥70, P4≥50. CLOSED: no impl, no parser change, no group_by/join/flat_map, no IO/capability, no app migration, no new OOF code; scalar fold protected both toolchains. proof: `experiments/fold_struct_accumulator_proof/verify_fold_struct_accumulator_p2.rb`; doc: `LANG-FOLD-STRUCT-ACCUMULATOR-P2-implementation-planning-v0.md`; card: `.agents/work/cards/lang/LANG-FOLD-STRUCT-ACCUMULATOR-P2.md`; next: LANG-FOLD-STRUCT-ACCUMULATOR-P3 (Rust TC) |
| [LANG-TEMPORAL-DATA-PATTERNS-P2](LANG-TEMPORAL-DATA-PATTERNS-P2-v0.md) | closed / docs-pattern proved — 59/59 PASS | Pure temporal data pattern note. `Temporal[T]`, `Snapshot[T]`, and `Trajectory[T]` are **documentation vocabulary only** for app-defined records/collections; not reserved identifiers, keywords, type constructors, primitives, or compiler/runtime features. Blessed examples: fixed-depth window, rewind as field shuffle, snapshot as explicit state slice, trajectory append as ordinary collection/data update, explicit tick compare over caller-supplied `Integer` ticks. Boundary: no new canon semantics; no `now()`, `DateTime`, clock, scheduler, storage, TBackend, IO, runtime time, app migration, or parser/compiler/runtime changes. PROP-022/028 remain separate `History[T]`/`BiHistory[T]` temporal-read authority. Links forward to fold-to-struct for record history aggregation and compose/entity for state-threading/update verbosity without absorbing either route. proof: `experiments/temporal_state_proof/verify_temporal_data_patterns_p2.rb`; doc: `LANG-TEMPORAL-DATA-PATTERNS-P2-v0.md`; card: `.agents/work/cards/lang/LANG-TEMPORAL-DATA-PATTERNS-P2.md` |
| [LANG-TEMPORAL-STATE-P1](LANG-TEMPORAL-STATE-P1-temporal-state-readiness-v0.md) | closed / proved — 48/48 PASS | Temporal-state readiness. ROUTE: **docs pattern** — shared proof-local `Temporal[T]`/`Snapshot[T]`/`Trajectory[T]` vocabulary, app-defined, **no canon change, no language primitive, no clock/IO**. HARD BOUNDARY: pure temporal **DATA** (prior values/snapshots/trajectories as records+collections; ticks = explicit `Integer` inputs) is **already expressible dual-clean today**; time-indexed temporal **READS** (`History[T]` with `now()`/`DateTime`/TBackend) = PROP-022/028 IO/clock side, **out of scope & closed**. SURVEYED: sim_framework (`TemporalInteger` fixed window, EvolveTemporal/Rewind1/TakeSnapshot/TimeTravel — dual-clean; SIM-P02 "no built-in Temporal[T] yet") + trade_robot (TR-P08 fold-to-struct; indicators.ig "fold() returns a single scalar"). KEYSTONE: pure window+trajectory compile dual-clean; folding history **into a record** fails **OOF-COL4** ("lambda return Integer ≠ accumulator Acc") — a **FOLD** limit, not temporal (scalar fold works). REAL LEVERS = separate already-routed cards: **LANG-FOLD-STRUCT-ACCUMULATOR-P1** (history aggregation) + **LANG-COMPOSE-ENTITY-P1** (removes window-shift/lens verbosity). NOT a primitive (keep app/library types), NOT a clock (no `now()` in any app), NOT primarily missing helpers (shapes are one-line records). CLOSED: no now(), no clock capability/TBackend, no persistence/DB/queue/file/network IO, no scheduler, no app migration, no compiler-managed history depth; LAB-IGNITER-LANG-IO-RUNTIME-P5 boundary intact. proof: `experiments/temporal_state_proof/verify_temporal_state_p1.rb`; doc: `LANG-TEMPORAL-STATE-P1-temporal-state-readiness-v0.md`; card: `.agents/work/cards/lang/LANG-TEMPORAL-STATE-P1.md`; next: optional docs note pointing at the two levers |
| [LANG-COMPOSE-ENTITY-P1](LANG-COMPOSE-ENTITY-P1-compose-entity-readiness-v0.md) | closed / proved — 49/49 PASS | Entity-composition readiness. ROUTE: **language proposal, held at readiness** (HOLD — author future PROP; no implementation, no keyword reserved). NAMED PATTERN: **state threading** (`input s : S … output s' : S`) + **config+state+behavior triad** — surveyed in trade_robot (`Portfolio`), sim_framework (`SimState`), arch_patterns (`AccountState`/`StateMachine`). DISTINCT FROM: dynamic dispatch (LAB-DYNAMIC-CONTRACT-DISPATCH-P2 — must not widen) + record-literal inference (MakeXxx factory anti-pattern — separate TC concern); manual unroll = related fold-over-struct gap, not required. KEYSTONE: the desugaring target (manual threading by pure contracts) **already compiles DUAL-CLEAN today** (Ruby 0 / Rust ok 0; fixture Counter/Increment/Reset/Drive) ⇒ an `entity` primitive can be **pure compile-time sugar, no new runtime authority** (copy-on-write, no mutation, no dynamic dispatch, no effect surface). BUILD ON: PROP-002 algebra (`>>`/`embed`) + PROP-016 shapes (⚠️ **`composes` keyword already reserved** → recommend distinct keyword **`entity`**) + LANG-TYPED-CONTRACT-REF `uses` (static behavior set, preserves P2 static dispatch) + PROP-031 modifier inference + PROP-044 variants + PROP-022/028 temporal — integrate, do not reinvent. 4 candidate approaches weighed (app-pattern/stdlib/entity-sugar/algebra-only); selected entity-sugar with app-pattern interim + algebra lowering substrate. CLOSED: no parser/compiler/runtime impl, no dynamic widening, no IO/capability/effect authority, no app edits, no `composes` redefinition, same-module v0, no inheritance. proof: `experiments/compose_entity_proof/verify_compose_entity_p1.rb`; doc: `LANG-COMPOSE-ENTITY-P1-compose-entity-readiness-v0.md`; card: `.agents/work/cards/lang/LANG-COMPOSE-ENTITY-P1.md`; next: LANG-COMPOSE-ENTITY-PROP-P2 (author full `entity` PROP) |
| [LANG-COMPILATION-REPORT-DIAGNOSTIC-ATTRIBUTION-P1](LANG-COMPILATION-REPORT-DIAGNOSTIC-ATTRIBUTION-P1-v0.md) | closed / implemented-proof — 60/60 PASS | Report-only multifile diagnostic attribution fix. `CompilationReport.enrich` now accepts optional typed context and maps report diagnostics back through `typed_program["contracts"][*]["type_errors"]` by rule/message/node/line, so later-contract diagnostics are no longer attributed to `contracts[0]`; `CompilerOrchestrator` passes `typed: typed`. Minimal fixture proves old stale attribution (`FirstClean/node:bad`) and fixed attribution (`LaterFail/node:bad`); duplicate-node diagnostics across contracts stay contract-specific; single-file reports unchanged. No TypeChecker/parser/classifier/emitter/assembler/runtime/app source changes; no new OOF codes; source-unit attribution and Rust report parity remain separate future routes. proof: `experiments/diagnostic_attribution_proof/verify_compilation_report_diagnostic_attribution_p1.rb`; card: `.agents/work/cards/lang/LANG-COMPILATION-REPORT-DIAGNOSTIC-ATTRIBUTION-P1.md` |
| [LANG-OPTIONAL-FIELD-PARTIAL-RECORD-P1](LANG-OPTIONAL-FIELD-PARTIAL-RECORD-P1-readiness-v0.md) | closed / readiness proved — 62/62 PASS | Optional-field partial record readiness. ROUTE: **HOLD implementation -> full PROP** before any compiler change. Ruby has parser code for `:question`, but the live Ruby lexer drops `?`, so `String?` currently parses as `optional=false`; Ruby `Classifier` can carry optional metadata, but `TypeChecker#type_shapes` drops it and `infer_record_literal` treats every expected field as required. Rust lexer/parser preserve `?` as `TokenType::Question` and `FieldDecl.optional: bool`, but Rust `build_type_shapes` drops optional metadata before `check_record_literal_shape`; both toolchains reject omitted optional-spelled fields with `OOF-TY0`. Wrong-type optional-spelled fields when present remain fail-closed (`OOF-TY0`) in both toolchains. `vector_editor` stays on explicit defaults (`path_pts: []`, `rect_data`, `text_data`) as the safe current pattern. CLOSED: no nullable runtime value, no `None`/`Unknown` default, no parser/typechecker/runtime/app migration, no output assignability relaxation, no IO. proof: `experiments/optional_field_proof/verify_optional_field_partial_record_p1.rb`; doc: `LANG-OPTIONAL-FIELD-PARTIAL-RECORD-P1-readiness-v0.md`; card: `.agents/work/cards/lang/LANG-OPTIONAL-FIELD-PARTIAL-RECORD-P1.md`; next: `LANG-OPTIONAL-FIELD-PARTIAL-RECORD-PROP-P2` |
| [LANG-IO-CAPABILITY-EXECUTOR-P2](LANG-IO-CAPABILITY-EXECUTOR-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans proof-local Ruby executor substrate under `experiments/io_capability_executor/`; Q1: NOT lib/ (temporal_access_runtime.rb precedent); Q2: 8 definitions — CapabilityPassport (7-field Struct), EffectReceipt (14-field Struct, string-keyed to_h), ExecutionContext (4-field Struct), RuntimeRefusal (4-field Struct), EffectResult (module, 7 factory methods), CapabilityExecutor (module/interface, NotImplementedError), CapabilityExecutorRegistry (register/fetch→nil/supports?/registered_families), StorageCapabilityExecutor (G1–G6 gates); Q3: registry keyed by capability class string e.g. `"IO.StorageCapability"`; fetch returns nil; Q4: Layer 1 = registry family check only; Layer 2 = RuntimeMachine 5-point passport verification (missing/revoked/expired/family_mismatch/authority_mismatch); Layer 3 = executor idempotency + G1–G6 gates; Q5: string-keyed Hashes throughout; EffectReceipt#to_h uses transform_keys(&:to_s); Q6: 2 authorized P3 files; sections A–P (≥55 checks); P1 80/80 regression; proof: `experiments/io_capability_executor_proof/verify_io_capability_executor_p2.rb` (86/86 PASS); next: LANG-IO-CAPABILITY-EXECUTOR-P3 |
| [LANG-IO-CAPABILITY-EXECUTOR-P1](LANG-IO-CAPABILITY-EXECUTOR-P1-capability-executor-v0.md) | closed / proved — 80/80 PASS | `CapabilityExecutor` interface + `CapabilityPassport` + 7-outcome `EffectResult` envelope + `EffectReceipt`; executor lookup via `escape_set` (not `requirements.json`); OOF compile-time vs runtime refusals classified; Storage read chosen for P2; Rack/ORM/AR/ambient-IO permanently closed; grounded by LAB-IGNITER-LANG-IO-RUNTIME-P1 (85/85); proof: `experiments/io_capability_executor_proof/verify_io_capability_executor_p1.rb`; next: LAB-IGNITER-LANG-IO-RUNTIME-P2 |
| [PROP-028](PROP-028-temporal-fragment-class-v0.md) | implemented-proof | TEMPORAL fragment class; classifier/typechecker/SemanticIR/assembler/load-guard proven (S3-R2..R5); parser syntax + production runtime pending |
| [PROP-022A](PROP-022A-temporal-manifest-errata-v0.md) | experiment-pass | Errata to accepted/PROP-022A: TEMPORAL manifest contract_index + fragment_summary; assembler PASS (S3-R5-C1) |
| [PROP-029](PROP-029-entrypoint-section-surface-v0.md) | authored-pending-review | Entrypoint as named evaluation/run profile; section as grouping-only source organization; no parser implementation yet |
| [PROP-030](PROP-030-executor-approval-token-contract-v0.md) | authored-pending-review | ExecutorApprovalToken contract; explicit approval as Gate 3 prerequisite; no executor or Gate 3 authorization |
| [PROP-030A](PROP-030A-temporal-scope-exclusion-errata-v0.md) | authored-pending-review | Errata to PROP-030: canonical `runtime.temporal_scope_exclusion` refusal for out-of-scope TEMPORAL executor artifacts |
| [PROP-031](PROP-031-contract-modifiers-v0.md) | experiment-pass | Contract modifiers: optional `pure/observed/effect/privileged/irreversible` prefix, implicit pure default, OOF-M1 only; parser/classifier/typechecker/SemanticIR proof PASS; no Effect Surface/Profile/authority/runtime enforcement |
| [PROP-032](PROP-032-assumptions-block-v0.md) | experiment-pass | `assumptions {}` block + `uses assumptions NAME`; Phase 1/2/3/4 compiler proofs accepted by S3-R36-C2-A; PROP-033 evidence validation and runtime receipts remain excluded |
| [PROP-033](PROP-033-via-profile-binding-v0.md) | experiment-pass | `via profile` source-level binding; profile_binding metadata propagates parse→classify→typecheck→SemanticIR; no runtime profile resolution or authority injection |
| [PROP-034](PROP-034-output-evidence-syntax-v0.md) | experiment-pass | Output `evidence [refs]` syntax/metadata; refs are opaque in v0; full evidence-chain validation, runtime evidence linkage, and cross-contract evidence remain excluded |
| [PROP-035](PROP-035-effect-surface-io-capability-v0.md) | experiment-pass | `capability` / `effect_binding` grammar and OOF-M2/M4/M5; IO.* types stay opaque; broader Effect Surface fields, delegation/schema validation, and runtime authority remain excluded |
| [PROP-036](PROP-036-compiler-profile-manifest-identity-v0.md) | accepted | `compiler_profile_id` manifest identity; accepted proposal-only by S3-R35-C3-A; separate implementation authorization required before code |
| [PROP-037](PROP-037-external-progression-service-liveness-v0.md) | accepted | External progression and service liveness semantics; accepted proposal-only by S3-R37-C3-A; descriptor/proof follow-ups only, no parser/runtime/fragment-class authorization |
| [PROP-038](PROP-038-compiler-profile-contract-v0.md) | accepted | `compiler_profile_contract`; accepted proposal-only by S3-R61-C3-A; proof-local implementation, internal validator extraction, report-only annotation, digest policy/proofs/errata, live validator implementation, strict-mode/refusal designs, proof-local result-shape, live implementation scope review, bounded internal-only strict-refusal implementation, live internal foundation acceptance, canon sync, and R86 Ch5/Ch7/language-spec sync are accepted in sequence through S3-R86-C4-A; public API/CLI, persisted reports/sidecars, loader/report, CompatibilityReport, runtime, Gate 3 widening, and production remain closed |
| [PROP-039](PROP-039-managed-local-recursion-and-loop-classes-v0.md) | experiment-pass | Managed local recursion and loop classes; gates 1+3+4+5+6+7+8 and recur()/body-semantics compiler checks closed; FiniteLoop/BudgetedLocalLoop/StructuralRecursion/FuelBoundedRecursion vocabulary + parse→classify→tc→SemanticIR pipeline; OOF-L1/R1/R2/R3/R4/R5/R6/R7 active experiment-pass; grammar_version="loop-v0"; ServiceLoop → PROP-037 exclusive; runtime recursion, VM stack, TCO, public/stable API, production runtime, and performance authority remain closed |
| [PROP-040](PROP-040-profile-declarations-v0.md) | experiment-pass | Module-level `profile <name> { authority: <modifier> }`; OOF-M7/M8 validate unknown profile and modifier/authority mismatch; no runtime profile injection/resolution, cross-module imports, or broader profile policy |
| [PROP-041](PROP-041-t2-structural-size-relation-v0.md) | experiment-pass / production compiler surface | T2 structural-size relation; proposal proof 48/48 and P7 production verification 48/48; `structural_size_v1` SemanticIR live; runtime execution, VM changes, and stable public API remain closed |
| [PROP-042](PROP-042-t3-numeric-measure-expressions-v0.md) | experiment-pass / production compiler surface | T3 numeric measure expressions; P5 production implementation closed 45/45 with T1/T2/R3 regressions clean; `numeric_measure_v0` live; runtime execution, VM stack, TCO, and public/stable API remain closed |
| [PROP-044](PROP-044-kind-discriminated-outcome-convention-and-sum-type-requirements-v0.md) | implemented through TypeChecker P5 | Kind-discriminated outcome convention plus variant/match path; P5 TypeChecker closed 75/75 with OOF-KIND1..5 active; P6 SemanticIR emitter pending; VM/runtime/public/stable sum-type authority closed |
| [PROP-045](PROP-045-source-intent-descriptor-and-queryable-contract-purpose-v0.md) | implemented metadata propagation P2 | Source-level `intent` descriptor; P2 parser/classifier/typechecker/SemanticIR metadata propagation closed 53/53; P3+ index/query tooling and stable query API pending; `intent` grants no capability, policy, or runtime authority |
| [LANG-EFFECT-SURFACE-RUNTIME-BRIDGE-P3](LANG-EFFECT-SURFACE-RUNTIME-BRIDGE-P3-implementation-proof-v0.md) | implementation proof — CLOSED 65/65 PASS | Bounded emitter implementation: 2 private methods (`io_effect_surface_stub` + `io_capability_escape_boundaries`) + 2 insertion points in `typed_contract_ir` and `typed_escape_boundaries`; `effect_surface_v0_stub` emitted for IO effect/privileged/irreversible contracts with capability decls; `io_capability` escape boundary entries emitted per capability; PROP-035 guard: `_stub` suffix distinguishes from full PROP-035 v0; nil/none for all pending-grammar fields (authority_ref/idempotency/receipt/failure); multi-capability fixture (2 bindings, 2 boundaries) proven; pure/observed contracts confirmed stub-free; P1 + P2 runners updated to fixed-state (all passing); no parser/classifier/typechecker/assembler/runtime changes; proof: `experiments/effect_surface_runtime_bridge_proof/verify_effect_surface_runtime_bridge_p3.rb` 65/65 PASS |
| [LANG-EFFECT-SURFACE-RUNTIME-BRIDGE-P2](LANG-EFFECT-SURFACE-RUNTIME-BRIDGE-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans minimal `effect_surface_v0_stub` bridge from existing `capability`/`effect_binding` typed declarations to SemanticIR; NO new parser productions needed; one authorized file: `semanticir_emitter.rb`; 2 insertion points: (1) optional `effect_surface` field after `escape_boundaries` in `typed_contract_ir`; (2) IO capability entries appended in `typed_escape_boundaries`; 2 new private methods: `io_effect_surface_stub` (emits `kind: "effect_surface_v0_stub"` with capability_bindings + nil grammar-dependent fields) + `io_capability_escape_boundaries` (emits `kind: "io_capability"` with `required_caps: ["IO.Capability"]`); PROP-035 guard: `_stub` suffix distinguishes from full v0 (upgrade-path clean); all 7 nil fields (authority_ref/idempotency_key_expr/receipt_type/failure_type + idempotency_mode:"none") confirmed pending PROP-035 grammar; affects_scope="external" and affects_target="IO.Capability" derivable from CR-001 sentinel without grammar; LAB-P3 may assume effect_surface present with non-empty capability_bindings + escape_boundaries with io_capability entries; proof: `experiments/effect_surface_runtime_bridge_proof/verify_effect_surface_runtime_bridge_p2.rb` 55/55 PASS |
| [LANG-EFFECT-SURFACE-RUNTIME-BRIDGE-P1](LANG-EFFECT-SURFACE-RUNTIME-BRIDGE-P1-v0.md) | authored — readiness / 52/52 PASS | Minimal Effect Surface subset required before first runtime IO execution; identifies 7 mandatory bridge fields (affects_scope/affects_target/authority_ref/idempotency_mode/idempotency_key_expr/receipt_type/failure_type); stages reversibility+compensation (compile-time OOF only); confirms SemanticIR gap (escape_boundaries empty, no effect_surface object); defines effect_surface_v0 design-target schema for PROP-035; 7-outcome EffectResult taxonomy with P15 load-bearing distinction (unknown_external_state ≠ failed); 5 runtime refusal codes disjoint from OOF codes; storage read recommended as P2 first family; no grammar/compiler/SemanticIR/runtime implementation |
| [PROP-046](PROP-046-storage-capability-query-execution-boundary-v0.md) | authored — proposal only | `IO.StorageCapability` query execution boundary; 9-field schema (allowed_sources/allowed_ops/row_limit/allow_include_all/read_allowed/write_allowed/deny_reason); 6-gate denial-as-data sequence (G4 clamps; G5 query_error); QueryExecutionReceipt shape; 15 design decisions locked; ESCAPE→STORAGE fragment classification; OOF-STORE1..5 candidates; no grammar changes needed for P2 (PROP-035 sufficient); DB/SQL/ORM/transactions/persistence permanently closed; write ops deferred (v1) |
| [LANG-STDLIB-IMPORT-SURFACE-P4](LANG-STDLIB-IMPORT-SURFACE-P4-rust-parity-proof-v0.md) | closed / proved — 57/57 PASS | Rust lab parity implementation of stdlib import validation in multifile.rs; OOF-IMP2 for unknown stdlib module / OOF-IMP3 for known module unknown name / OOF-IMP6 for user stdlib.* namespace shadow; stdlib_module_table() embedded via include_str! at compile time; append now importable (inventory current); advanced_logistics/vector_editor/arch_patterns/decision_tree all move past Rust stdlib import blocker (advanced_logistics=ok; others=OOF-TY0); Rust parser does not treat label as keyword (decision_tree reaches OOF-TY0, not DT-P02); no TC/SemanticIR/VM/Ruby/inventory changes |
| [LANG-STDLIB-IMPORT-SURFACE-P3](LANG-STDLIB-IMPORT-SURFACE-P3-ruby-implementation-proof-v0.md) | closed / proved — 62/62 PASS | Ruby canon implementation of stdlib import surface in MultifileResolver; OOF-IMP2 for unknown stdlib module / OOF-IMP3 for known module unknown name / OOF-IMP6 for user stdlib.* namespace shadow; stdlib_module_table memoized from stdlib-inventory.json; import clears before TC (unchanged); advanced_logistics stdlib import unblocked; vector_editor/arch_patterns move to OOF-IMP3(append); decision_tree blocked at parse (DT-P02 label keyword); UTF-8 inventory read required; single-file compile bypasses MultifileResolver (two-file fixtures); no TC/SemanticIR/VM/Rust/inventory changes |
| [LANG-STDLIB-IMPORT-SURFACE-P2](LANG-STDLIB-IMPORT-SURFACE-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans bounded Ruby canon implementation of stdlib import resolution in `MultifileResolver`; RUBY ONLY (Rust in P4); THREE INSERTIONS in `multifile_resolver.rb`: (1) stdlib guard inside `validate_imports` before `by_module` lookup — `stdlib_path?` intercept routes `stdlib.*` paths to table check, OOF-IMP2 for unknown module, OOF-IMP3 per-name for known module; (2) four private helpers: `stdlib_path?` / `stdlib_module_known?` / `stdlib_name_known?` / `stdlib_module_table` (memoized; loads `../../docs/spec/stdlib-inventory.json` relative to `__dir__`); (3) OOF-IMP6 guard in `resolve` before `by_module` construction rejects `module stdlib.*` user declarations; TABLE SHAPE: `Hash[String→Set[String]]` keyed on module path (stdlib.collection/{map,filter,count} / stdlib.text/{14 names} / stdlib.map/{4 names} / stdlib.option/{or_else} / stdlib.bool/{empty} / stdlib.numeric/{empty}); GAPS: fold/sum/append/concat not importable (not in inventory); APP IMPACT: advanced_logistics filter→OK; vector_editor map→OK/append→OOF-IMP3; decision_tree filter→OK/append→OOF-IMP3; IMPORT CLEARS BEFORE TC: already the case (merge_units sets imports:[]; no TC change needed); AUTHORITY PROOF: Section E 7 checks — SIR shape unchanged, no capability/profile fields, no stdlib source_units; RUST: include_str!("../../../igniter-lang/docs/spec/stdlib-inventory.json") + build_stdlib_table() in multifile.rs (P4); proof matrix ≥61 checks / 7 sections (A regression 15 / B stdlib happy 10 / C OOF-IMP2+OOF-IMP6 7 / D OOF-IMP3 8 / E authority 7 / F app fixtures 9 / G table derivation 5); no TypeChecker/SemanticIR/assembler/inventory/VM/parser/classifier changes |
| [LANG-STDLIB-IMPORT-SURFACE-P1](LANG-STDLIB-IMPORT-SURFACE-stdlib-import-surface-v0.md) | authored — boundary decisions made / pending review | Stdlib import surface v0: defines how `import stdlib.collection.{ map }` works without package/runtime authority; Q1=built-in stdlib module table (derived from inventory; `stdlib.*` namespace sealed; consulted before user module table); Q2=source aliases only (import validates + clears; TypeChecker dispatch unchanged); Q3=NO capability authority (name availability ≠ authority); Q4=stdlib_surface_digest is identity anchor for importable surface (profile pinning = future PROP-036/038 slot); Q5=OOF-IMP2 (unknown stdlib module) + OOF-IMP3 (known module unknown name); no new OOF codes; Q6=NO SIR name change (canonical qualified names are entry-contract rule, not import rule); `append` = OOF-IMP3 today (not in inventory — VE-P02 → LANG-STDLIB-COLLECTION-APPEND-P1); "import validates, not gates" v0 stance; no parser/TC/SemanticIR/resolver/inventory/VM changes; next LANG-STDLIB-IMPORT-SURFACE-P2 implementation planning |
| [PROP-IMPORT-RESOLUTION](PROP-IMPORT-RESOLUTION-import-resolution-and-multifile-compilation-unit-v0.md) / [P2](PROP-IMPORT-RESOLUTION-P2-implementation-planning-v0.md) / [P2A](PROP-IMPORT-RESOLUTION-P2A-diagnostic-namespace-decision-v0.md) / [P4](PROP-IMPORT-RESOLUTION-P4-ruby-canon-parity-decision-v0.md) / [P5](PROP-IMPORT-RESOLUTION-P5-ruby-canon-implementation-proof-v0.md) | authored + planned + diagnostic namespace accepted + Ruby parity implemented/proved | `import` = compile-time name resolution only; N `.ig` files form one logical compilation universe and one `.igapp`; P2 selects compiler-driver pre-pass + merged logical universe; P2A reserves `OOF-IMP*`; Rust-lab P3 proved implementation 83/83 as evidence; P4 decides Ruby canon parity ready; P5 implements bounded Ruby `MultifileResolver` + internal orchestrator `compile_sources`, `source_units` report/SemanticIR/manifest evidence, final diagnostics, deterministic identity, and entrypoint coexistence; 99/99 PASS; no CLI/public API/package/visibility/stdlib/runtime/VM/capability authority |
| [PROP-ENTRYPOINT](PROP-ENTRYPOINT-explicit-entrypoint-declaration-v0.md) | authored-pending-review - proposal only | Explicit top-level `entrypoint ContractName` declaration; zero-or-one default entrypoint per compilation unit; `.igapp` manifest binding; separates language declaration from CLI/debugger runner policy; narrows PROP-029 entrypoint surface and carries no `section`, app framework, visibility, package, scheduler, capability, parser/compiler/VM, or public API authority |
| [PROP-ENTRYPOINT-P2](PROP-ENTRYPOINT-P2-parser-manifest-planning-v0.md) | implementation-planning-only - ready for P3 | Plans bounded Ruby `igniter-lang` parser/typechecker/SemanticIR/manifest implementation for `entrypoint ContractName`; AST and manifest shapes chosen; OOF-EP1/2 language diagnostics planned, OOF-EP3/6 tool-mode diagnostics deferred, OOF-EP4 multi-file ambiguity deferred; no CLI/VM/app framework/visibility/package/capability/public API authority |
| PROP-ENTRYPOINT-P3 | closed - proved / bounded Ruby implementation | Ruby single-file pipeline implements top-level `entrypoint ContractName` metadata through parser, classifier, typechecker, SemanticIR, `.igapp` manifest, and artifact hash material; `OOF-EP1`, `OOF-EP2`, and `OOF-EP5` proven; zero-entrypoint library remains valid; 53/53 PASS; no CLI/VM/app framework/visibility/package/import/capability/public API authority |
| [LANG-TYPED-CONTRACT-REF](LANG-TYPED-CONTRACT-REF-typed-contract-reference-declaration-v0.md) | authored — pending review | `uses ContractName` contract body declaration; typed, static, inspectable dependency edge without invocation, capability grant, or fragment class change; follows `uses assumptions NAME` (PROP-032) syntactic precedent — 1-token lookahead disambiguation; metadata-only in v0 (`contract_refs` field in SemanticIR, `dependency_edges` in manifest, enters artifact hash); `OOF-REF1/2/4` active diagnostics; `OOF-REF3/5` reserved; `call_contract` not deprecated (stringly pressure source with upgrade path); forms lowering substrate (TH-1 conservativity path); no parser/compiler/VM/capability/visibility/package/public API implementation authorized; grounded by LAB-TYPED-CONTRACT-REF-P1 (58/58 PASS); next LANG-TYPED-CONTRACT-REF-PROP-P2 implementation planning |
| [LANG-TYPED-CONTRACT-REF-P2](LANG-TYPED-CONTRACT-REF-P2-implementation-planning-v0.md) | implementation-planning-only — READY FOR P3 | Plans bounded Ruby `igniter-lang` implementation of `uses ContractName` through parser + classifier + typechecker + SemanticIR emitter + assembler; same-module only in v0; `contract_refs` per-contract field; `dependency_edges` manifest field; OOF-REF1/2/4 exact triggers; ~110–160 lines across 5 files; proof matrix ≥60 checks; PROP-IMPORT-RESOLUTION-P5 + PROP-ENTRYPOINT-P3 + LAB-TYPED-CONTRACT-REF-P1 regressions listed; cross-module/forms/VM/runtime/call_contract/capability closed |
| [LANG-TYPED-CONTRACT-REF-P3](LANG-TYPED-CONTRACT-REF-P3-ruby-implementation-proof-v0.md) | closed / proved — 67/67 PASS | Bounded Ruby canon implementation of `uses ContractName`; 1-token lookahead parser branch; classifier `"metadata"` fragment + `contract_fragment_for` exclusion; same-module typechecker registry + OOF-REF1/2/4; SemanticIR `contract_refs` enters `contract_ref` hash; assembler `dependency_edges` with `execution_dependency: false`; 11 fixtures; regressions: PROP-ENTRYPOINT-P3 53/53 + PROP-IMPORT-RESOLUTION-P5 99/99 + LAB-TYPED-CONTRACT-REF-P1 58/58; cross-module/forms/VM/capability/call_contract closed |
| [LANG-TYPED-CONTRACT-REF-P4](LANG-TYPED-CONTRACT-REF-P4-cross-module-resolution-planning-v0.md) | implementation-planning-only — READY FOR P5 | Plans bounded cross-module `uses` resolution turning P3's OOF-REF2 deferred gap into actual resolution; 3 resolution paths: qualified (`uses Mod.Contract`) / imported unqualified (`import Mod` + `uses Contract`) / local unchanged; OOF-REF2 narrowed to ambiguous-import-only; OOF-REF1 extended to unknown-module + unknown-contract-in-known-module; OOF-REF4 extended to cross-module cycles; new SIR field `resolution_kind: "local"/"qualified"/"imported"/"unresolved"`; new manifest fields `from_module`/`to_module`/`resolution_kind` on `dependency_edges`; cross-module registry built by MultifileResolver classifying each unit before merge; TypeChecker receives `cross_module_registry:` kwarg (default `{}` preserves single-file compat); selective imports respected; local shadows imported (silent, no diagnostic); 5 authorized files; proof matrix ≥71 top-level checks across 12 sections + 3 embedded regression suites; parser/VM/runtime/forms/call_contract/capability/CLI closed |
| [LANG-STDLIB-ENTRY-CONTRACT-P3](LANG-STDLIB-ENTRY-CONTRACT-P3-inventory-proof-v0.md) | closed / proved — 76/76 PASS | Stdlib inventory JSON `docs/spec/stdlib-inventory.json` with 24 entries (text×14 / map×4 / option×1 / collection×1 / orphans×4); schema-valid, digest-stable, bidirectional dispatch consistent; `or_else` sole legacy_sir; map.empty Unknown note; operator-lowered orphans visible; stdlib_surface_digest algorithm proved deterministic (sort+strip+sha256); section I compile-skipped gracefully; no parser/TC/emitter/VM changes; P4 will embed digest + or_else migration + Rust parity |
| [LANG-STDLIB-ENTRY-CONTRACT-P2](LANG-STDLIB-ENTRY-CONTRACT-P2-implementation-planning-v0.md) | closed / planning — READY FOR P3 | Registry implementation plan: single hand-authored JSON `docs/spec/stdlib-inventory.json` (Ch8-referenced; digest over canonical re-serialization, never raw bytes); schema finalized (legacy_sir field + global alias uniqueness + purity⟺capability⟺ESCAPE consistency rule); entry_digest/surface_digest exact formulas (sorted, digest-stripped, IMPORT-P5 discipline); derived-dispatch Option A for P3 (manual tables verified both directions — stray dispatch name = proof FAIL = anti-accretion gate); first 24 entries: text×14 + map×4 + or_else (sole legacy_sir, counted-grandfather check) + collection.count + 4 orphan records; or_else canonical = stdlib.option.or_else NOT unwrap_or (Ch8 Option vs Result ops; NEW DRIFT: Rust TC conflates them in one match arm, typechecker.rs:2696 → OPTION-P1); fixtures never change (source_alias permanent); emission migration = P4 separately authorized; proof matrix 68 checks / 10 sections; profile pinning = future stdlib_surface slot via separate PROP amendment |
| [LANG-STDLIB-ENTRY-CONTRACT](LANG-STDLIB-ENTRY-CONTRACT-stdlib-entry-contract-v0.md) | authored-pending-review | Stdlib Entry Contract v0: every stdlib entry = sealed language-owned semantic claim (identity/signature/authority/lowering-evidence/proof-lineage record); canonical_name always qualified `stdlib.<category>.<fn>`; semantic_ir_name MUST equal canonical_name (kills bare-name drift; bare `or_else` = sole grandfathered legacy_sir violation with P2 migration route); aliases append-only churn ledger (source_alias/legacy_sir/lab_only/rejected); status × stability{semantic,lowering,compatibility} as independent axes (Ch8 collection ops = canon+kernel-only; `stdlib.option.wrap` = orphaned+dual-toolchain); authority grant fields schema-absent — effectful entries consume capability params (ESCAPE tier), never produce authority; `entry_digest` + `stdlib_surface_digest` (canonical-JSON sorted records, PROP-036 profile-pin pattern) design-only; category routing recorded (text/map READY, option/result reconciliation, collection SPLIT, numeric/datetime HOLD, outcome OPEN-as-convention → LAB-STDLIB-OUTCOME-P1, bool DO-NOT-CREATE); proof bar per claim level incl. dual-toolchain parity; no implementation/renaming/registry-build/package authority |
| [LANG-TYPED-CONTRACT-REF-P5](LANG-TYPED-CONTRACT-REF-P5-cross-module-implementation-proof-v0.md) | closed / proved — 71/71 PASS | Bounded Ruby canon implementation of cross-module `uses` resolution; closes OOF-REF2 gap from P3/P4; 3-path resolution: PATH 1 qualified (`uses Mod.Contract` → `resolution_kind: "qualified"`), PATH 2a local-first with per_contract_module attribution check, PATH 2b scan import scope (0→OOF-REF1, 1→`resolution_kind: "imported"`, ≥2→OOF-REF2 narrowed); MultifileResolver adds `build_cross_module_registry`/`build_per_module_imports`/`build_per_contract_module` from pre-merge units (merge destroys attribution); TypeChecker receives all three as per-call kwargs (default `{}` preserves single-file compat); SIR `resolution_kind` field + short `contract_name` (not full dotted target) + `module_name` for resolved refs; assembler `from_module`/`to_module`/`resolution_kind` on `dependency_edges`; DFS cycle detection for OOF-REF4; attach_source_units! nil-guard; 11 cross-module fixtures; OOF-DECL-DUP-CONTRACT constraint documented (ambiguity tests use direct TypeChecker invocation); sections A–L; regressions PROP-ENTRYPOINT-P3 53/53 embedded; parser/VM/runtime/forms/call_contract/capability/CLI closed |
| [LANG-FORM-VOCABULARY](LANG-FORM-VOCABULARY-explicit-dictionary-and-resolution-v0.md) | authored — pending review | Explicit form vocabulary and order-independent resolution; governance boundary for named form words as conservative elaboration over typed-ref substrate; `speaks VocabularyPath` at module level (preferred syntax direction; grammar deferred to P2); V-1..V-9 coherence rules (no ambient leakage, ownership rule, no first-wins, import-order independence, typed-ref anchor required, InvocationIntent equality, no_form propagation, fragment class invariant, primitive reservation); TH-1..TH-6 mapped to concrete rules and prior lab proof evidence; OOF-FORM1..OOF-FORM9 namespace reserved; form_vocabularies/form_resolutions/resugaring_trace SIR design shapes defined; Rust orphan lineage classified (form_registry.rs/form_resolver.rs/FormKind×7/F-01..F-06 kept; AccumulatorRef/FormShape inheritance/MultiKeyword-user deferred); 13 required explicit answers all provided; next LANG-FORM-VOCABULARY-PROP-P2 implementation planning; parser/typechecker/SemanticIR/VM/runtime/public syntax/package/capability/call_contract/macro closed |
| [LANG-STDLIB-OUTCOME](LANG-STDLIB-OUTCOME-outcome-helper-predicates-v0.md) | authored-pending-review | `stdlib.outcome` v0: 7 accepted entries (kind accessor + 6 PROP-047 stable-term predicates); all pure/total/authority_surface:none/proof-local; is_retryable deferred (dispatch context required; 3 design options for P2); route() permanently rejected (policy encoding = runtime authority); domain-local kinds preserved (false from is_*; passthrough from kind()); OOF-OUT1..OUT4 reserved; input Map[String,String] (KDR-like); no Outcome[T,E]; no variant enforcement; grounded by LAB-STDLIB-OUTCOME-P1 66/66 PASS + PROP-047-P2 + LAB-FAILURE-TAXONOMY-P4; LANG-STDLIB-ENTRY-CONTRACT schema applied; semantic_ir_name == canonical_name enforced; next LANG-STDLIB-OUTCOME-PROP-P2 implementation planning |
| [LANG-STDLIB-OUTCOME-P2](LANG-STDLIB-OUTCOME-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans bounded Ruby canon implementation of `stdlib.outcome` helpers in TypeChecker only; SCOPE=Option A (TypeChecker+SIR; stdlib-inventory.json absent — LANG-STDLIB-ENTRY-CONTRACT-P3 independent parallel track); `OUTCOME_STDLIB_FNS` hash (7 entries; source aliases `outcome_kind`/`outcome_is_denied`/etc. per map_get prefix pattern); `when *OUTCOME_STDLIB_FNS.keys` dispatch case in `infer_call` after MAP case; shared `infer_outcome_call` private method; SIR naming automatic via generic `semantic_expr` (zero emitter changes); OOF-OUT1 for non-Map argument; OOF-TY0 for arity mismatch; is_retryable/route → OOF-TY0 (not registered); domain-local preservation at TC level (any Map accepted; no content inspection; runtime semantics deferred to P4+); proof matrix ≥60 checks / 9 sections (A–I); authorized files: `typechecker.rb` + `experiments/stdlib_outcome_proof/verify_stdlib_outcome_p3.rb`; 7 stdlib-inventory.json entry records ready for LANG-STDLIB-ENTRY-CONTRACT-P3; ~40–60 lines in one file |
| LANG-STDLIB-OUTCOME-P3 | closed / proved — 60/60 PASS | Bounded Ruby canon implementation of `stdlib.outcome` helpers in TypeChecker; `OUTCOME_STDLIB_FNS` constant (7 entries) + `when *OUTCOME_STDLIB_FNS.keys` dispatch in `infer_call` + shared `infer_outcome_call` private method; OOF-OUT1 for non-Map arg / OOF-TY0 for arity; SIR `fn` = canonical qualified name via zero-change generic `semantic_expr`; is_retryable/route → OOF-TY0 (not registered); domain-local preservation at TC level (any Map accepted; kind values not inspected); 9 sections A–I; regressions: string_core_proof 60/60 + typed_contract_ref_p5 71/71; zero emitter/parser/VM/Rust changes; next LANG-STDLIB-OUTCOME-PROP-P4 Rust parity or inventory integration |
| LANG-STDLIB-COLLECTION-RANGE-P3 | closed / proved — 50/50 PASS | Rust parity for `stdlib.collection.range`; OOF-COL1 arity guard added to Rust TC `"range" =>` arm (args.len()!=2; resolved_type=Collection[Integer] set first — no OOF-TY1 cascade); `("range","stdlib.collection.range")` added to emitter `COLLECTION_HOF_OPS` + `"range"` added to `semantic_expr_for_compute` delegate `matches!` (both required: standalone compute routes through `semantic_expr_for_compute` which only delegates to `semantic_expr` for listed fns); `build_pipeline` range arm unchanged (pipeline IR shape); inventory `lowering_status`→`dual-toolchain`; P2 proof B-03 fixed-state (ruby-only OR dual-toolchain); Rust build clean; 8 sections A–H; proof runner: `igniter-lab/igniter-compiler/verify_stdlib_collection_range_p3.rb`; proof packet: `LANG-STDLIB-COLLECTION-RANGE-P3-rust-parity-proof-v0.md`; TRACK CLOSED: dual-toolchain |
| LANG-STDLIB-COLLECTION-RANGE-P2 | closed / proved — 50/50 PASS | Bounded Ruby TC implementation of `stdlib.collection.range`; `when "range"` dispatch arm in `infer_call` (after `when "char_at"`) + `infer_range_call` private method; output `Collection[Integer]` on all paths; OOF-COL1 on arity ≠ 2 (no cascade — Collection[Integer] returned even on error); arg types Unknown-permissive in v0 (no OOF-COL2 — args are Integer scalars not Collections); inventory entry added (36 entries; `lowering_status: "ruby-only"`; `lifecycle_status: "lab-implemented"`); `stdlib_surface_digest` recomputed; no emitter / no Rust / no parser / no app changes; bloom_filter example.ig still has manual slot pattern (P2 is TC proof only); 8 sections A–H; proof runner: `experiments/stdlib_collection_range_proof/verify_stdlib_collection_range_p2.rb`; proof packet: `LANG-STDLIB-COLLECTION-RANGE-P2-ruby-proof-v0.md`; next LANG-STDLIB-COLLECTION-RANGE-P3 Rust parity |
| [LANG-STDLIB-COLLECTION-RANGE-P1](LANG-STDLIB-COLLECTION-RANGE-P1-proposal-readiness-v0.md) | closed / proved — 42/42 PASS | Proposal + readiness proof for `stdlib.collection.range`; DECISION: `range(start, stop)` two-arg form (ACCEPT) over `range(count)` (REJECT); exclusive `[start, stop)` interval; output `Collection[Integer]` — concrete, not generic; OOF-COL1 arity only (no new codes); Ruby TC gap confirmed (OOF-TY0 "Unknown function: range"); Rust TC already implements; bloom_filter pressure: 31 manual computes → 2 with `map(range(0,16), ...)`; canon usage: `stdlib_extension.ig` + `availability_projection.ig` both use two-arg form; next LANG-STDLIB-COLLECTION-RANGE-P2 Ruby TC dispatch |
| [LANG-STRING-TEXT-ALIAS-P2](LANG-STRING-TEXT-ALIAS-P2-concat-string-proof-v0.md) | closed / proved — 52/52 PASS | Input-type-driven concat return; three files: `typechecker.rb` + `typechecker.rs` + `emitter.rs`; Ruby: `infer_concat_call` — String first arg + 2 args → infer second → if also String → `typed_expr("call", type_ir("String"), deps, "fn"=>"stdlib.string.concat")`; Rust infer_expr: `both_string` guard in text path → `type_ir("String")`; Rust `rewrite_concat_calls`: String+String → `"stdlib.string.concat"`; Rust emitter: extend (B) condition + String `resolved_type` branch; no TEXT_STDLIB_FNS / no text_arg_compatible? / no structurally_assignable? / no record literal / no inventory changes; SIM-P10/P11 RESOLVED; sim_framework Ruby DUAL-CLEAN; string_core 60/60 PASS regression; Rust build clean; sections A–K |
| [LANG-STRING-TEXT-ALIAS-P1](LANG-STRING-TEXT-ALIAS-P1-string-text-alias-v0.md) | closed / proved — 47/47 PASS | String and Text are distinct nominal types; gap is stdlib-return-type only: `concat(String,String) → Text` (should be String); SIM-P10 OOF-TY0 + SIM-P11 OOF-TY1 cascade both reproduced via minimal fixture; narrowest P2 route = ~5 lines in `infer_concat_call` (Ruby) + ~5 lines Rust `concat` arm; no new OOF codes; no alias; no structural changes; Rust `++` operator already has String+String→String path (4215–4217); P2 extends to `concat` function form; 7 questions answered; 10 sections A–J |
| [LANG-STDLIB-STRING-SURFACE-P1](LANG-STDLIB-STRING-SURFACE-P1-string-surface-v0.md) | closed / proved — 55/55 PASS | Minimal `stdlib.string` surface to unblock igniter_parser IP-P01; `stdlib.string.char_at(String, Integer) -> String`; byte-indexed 0-based; `stdlib.string` new namespace (not reuse of `stdlib.text`); inventory-only change clears OOF-IMP2 in both toolchains; `substring` deferred to LANG-STDLIB-STRING-SLICE-P1; String/Text alias deferred to LANG-STRING-TEXT-ALIAS-P1; no compiler/app changes in P1; P2=Ruby TC dispatch; P3=Rust TC+VM; proof 55/55 PASS |
| [LANG-STDLIB-STRING-SUBSTRING-P2](LANG-STDLIB-STRING-SUBSTRING-P2-implementation-proof-v0.md) | closed / proved — 75/75 PASS | Dual-toolchain implementation of `stdlib.string.substring(String, Integer, Integer) -> String`; FOUR PRODUCTION FILES: (1) inventory entry added (37 entries; lifecycle=lab-implemented; lowering=dual-toolchain; digest recomputed `160999932e9e1dde…`); (2) Ruby TC `when "substring"` arm + `infer_substring_call` private method after `infer_char_at_call` (qualified fn inline → zero emitter changes); (3) Rust TC `"substring"` arm after `"char_at"` arm (is_resolved=true; String on all paths; OOF-TY0 for arity/arg1/arg2/arg3); (4) Rust emitter `STRING_STDLIB_OPS` add `"substring"` entry + delegation guard `fn_val == "substring"` in `semantic_expr_for_compute`; OOF-TY0 only (no new codes): arity≠3 early-return / arg1 not String·Unknown / arg2 not Integer·Unknown / arg3 not Integer·Unknown; String returned all paths incl. OOF; Unknown permissive all 3 positions; `(source, start, length)` byte-based 0-based; no semanticir_emitter.rb / multifile_resolver.rb / multifile.rs / parser changes; char_at regression ok/0 both toolchains; 15 sections A–O; next IP-P05 igniter_parser source migration |
| [LANG-STDLIB-STRING-SUBSTRING-P1](LANG-STDLIB-STRING-SUBSTRING-P1-substring-v0.md) | closed / proved — 49/49 PASS | Proposal + readiness proof for `stdlib.string.substring(String, Integer, Integer) -> String`; second stdlib.string entry after char_at (P3); IP-P05 PENDING-BEHIND-P01 confirmed (lexer.ig has no substring; report.md names it future); DECISIONS: (source,start,length) length-form not stop; byte-based 0-based (consistent with char_at + report.md); totality=partial out-of-bounds unspecified v0; authority_surface=none/pure/deterministic; no Text involvement; OOF-TY0 only (no new codes) — arity≠3 early-return / arg1 not String·Unknown / arg2 not Integer·Unknown / arg3 not Integer·Unknown; String returned all TC paths incl. OOF; Unknown permissive all 3 positions; P2=dual-toolchain no split (mirrors char_at P3 decision); insertion points confirmed: Ruby TC after when "char_at" arm / Rust TC after "char_at" arm / Rust emitter add "substring" to STRING_STDLIB_OPS + delegation guard in semantic_expr_for_compute; inventory probe: stdlib.string.{char_at,substring} both importable after entry added; no multifile_resolver.rb / multifile.rs / semanticir_emitter.rb / parser / app changes; 7 sections A–G 49 checks; proof runner: `experiments/stdlib_string_surface_proof/verify_stdlib_string_substring_p1.rb`; proposal: `LANG-STDLIB-STRING-SUBSTRING-P1-substring-v0.md`; card: `igniter-lang/.agents/work/cards/lang/LANG-STDLIB-STRING-SUBSTRING-P1.md`; next LANG-STDLIB-STRING-SUBSTRING-P2 dual-toolchain implementation |
| [LANG-STDLIB-STRING-SURFACE-P3](LANG-STDLIB-STRING-SURFACE-P3-implementation-proof-v0.md) | closed / proved — 70/70 PASS | Dual-toolchain implementation of `stdlib.string.char_at(String, Integer) -> String`; FOUR PRODUCTION FILES: (1) inventory entry added (36 entries; lifecycle=lab-implemented; lowering=dual-toolchain; digest recomputed `cfe520dc…`); (2) Ruby TC `when "char_at"` arm + `infer_char_at_call` private method (qualified fn inline → zero emitter changes); (3) Rust TC `"char_at"` arm (is_resolved=true; String on all paths); (4) Rust emitter `STRING_STDLIB_OPS` rewrite + `semantic_expr_for_compute` delegation guard (KEY DISCOVERY: compute path must explicitly delegate to reach `STRING_STDLIB_OPS` rewrite — same pattern as LANG-STDLIB-COLLECTION-MAP-FILTER-P4); OOF-TY0 only (no new codes): arity≠2 early-return / arg1 not String·Unknown / arg2 not Integer·Unknown; String returned all paths incl. OOF; Unknown permissive both positions; no semanticir_emitter.rb / multifile_resolver.rb / multifile.rs / parser changes; concurrent LANG-STDLIB-COLLECTION-RANGE-P2 added range entry (35→36); `.igapp` is directory package — SIR read from `semantic_ir_program.json` inside; 15 sections A–O; next LANG-STDLIB-STRING-SLICE-P1 (substring) |
| [LANG-STDLIB-STRING-SURFACE-P2](LANG-STDLIB-STRING-SURFACE-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans dual-toolchain implementation of `stdlib.string.char_at`; PLANNING DECISION: READY FOR P3 dual-toolchain in one card (no split — symmetric changes, splitting leaves stranded TC state); INVENTORY: 1 new entry `stdlib.string.char_at` / source_alias `char_at` / lifecycle=lab-proposed / lowering=not-implemented / upgraded to lab-implemented+dual-toolchain when P3 proof passes / projected digest `3e51bf95…`; RUBY TC: `when "char_at"` arm in `infer_call` after `when "is_empty", "non_empty"` (~line 1038) + `def infer_char_at_call` method after `infer_is_empty_call` (~line 2782); qualified fn inline → zero Ruby emitter changes; RUST TC: `"char_at" =>` arm after `"is_empty" | "non_empty"` (~line 2780); is_resolved=true; String on all paths; RUST EMITTER: STRING_STDLIB_OPS const + rewrite block after COLLECTION_HOF_OPS (~line 810); fn-only rewrite to `stdlib.string.char_at`; NO CHANGES: semanticir_emitter.rb / multifile_resolver.rb / multifile.rs; DIAGNOSTICS: OOF-TY0 only — arity≠2 early-return / arg1 not String·Unknown / arg2 not Integer·Unknown; String returned all paths; no text_arg_compatible?; NO NEW OOF CODES; P3 PROOF: ≥60 checks / 14 sections A–N (inventory / Ruby import / Rust import / Ruby TC / Rust TC / Ruby SIR / Rust SIR / arity diags / non-String arg1 / non-Integer arg2 / igniter_parser lexer OOF-IMP2 gone / substring deferred / stdlib.text unchanged / authority); proof runner: `experiments/stdlib_string_surface_proof/verify_stdlib_string_surface_p3.rb`; 4 production files + 1 proof runner |
| [LANG-OUTPUT-TYPE-ASSIGNABILITY-P1](LANG-OUTPUT-TYPE-ASSIGNABILITY-P1-structural-output-assignability-v0.md) | authored — pending review | Structural output type assignability; replaces shallow `type_name()` equality at output boundary with recursive `structurally_assignable?(actual, expected)`; **Unknown policy: REJECT** — `Unknown → T` fails at any depth unless expected is explicitly `Unknown`; **OOF-TY1 new** — output structural failure code (uses `type_display()` for full parameterized message); OOF-TY0 removed from output path (continues for inline errors); LAB-RACK-P9 superseded — Rust TC guard removed; impact: `rule_engine` ExecuteRules `Collection[Unknown] → Collection[RuleDecision]` output becomes OOF-TY1 on both TCs (blocked until validation receipt or quarantine semantics); collection Unknown follows same recursive rule (Collection[Unknown] → Collection[T] FAILS at depth-1 Unknown→T check); permissive direction preserved (T → Unknown PASSES; concrete → Unknown-expected PASSES at any depth); grounding: LAB-UNKNOWN-OUTPUT-COERCION-P1 36/36 + LAB-OUTPUT-TYPE-PARAMETER-CHECK-P1 38/38; authorized surfaces P2: `typechecker.rb` + `typechecker.rs` (~20 lines each) + proof runner; no parser/emitter/assembler/inventory/VM/dynamic-dispatch/receipt changes; proof matrix ≥70 checks / 12 sections (A unit-Ruby 8/B unit-Rust 6/C outer-mismatch 5/D Unknown→concrete 6/E param-mismatch 6/F nested 5/G permissive-PASS 7/H Unknown→Unknown 4/I rule_engine 5/J regression 7/K OOF-TY0-absent 5/L authority 6) |
| [LANG-OUTPUT-TYPE-ASSIGNABILITY-P2](LANG-OUTPUT-TYPE-ASSIGNABILITY-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Implementation plan for structural output assignability; exact insertion points: Ruby `typechecker.rb` line 413 (output boundary replace) + after line ~1385 (2 new methods: `structurally_assignable?` + `structural_mismatch`); Rust `typechecker.rs` lines 1236–1244 (boundary replace + LAB-RACK-P9 removal) + after line ~2045 (2 new methods: `structurally_assignable` + `type_display`); D2: actual Unknown→false at all depths / D3: expected Unknown→true / D4: OOF-TY1 new / D5: OOF-TY0 removed from output path / D6: LAB-RACK-P9 removed; `blocking_rule_present?` preserved unchanged; OOF-TY1 NOT in blocking list; `type_display` renders `Collection[Integer]`/`Map[String,Text]` etc.; Ruby `type_display` already exists (line 1351); Rust `type_display` new; `rule_engine` ExecuteRules `Collection[Unknown]→Collection[RuleDecision]` will emit OOF-TY1 after P3 (previously SILENT); RE-P04 stays HOLD/SAFETY-HIGH; P3 Ruby: ≥62 checks/11 sections; P4 Rust: ≥45 checks/9 sections; authorized: `typechecker.rb` + `typechecker.rs` only (~12 Ruby + ~18 Rust new lines); no parser/emitter/assembler/inventory/VM changes |
| [LAB-UNKNOWN-OUTPUT-COERCION-P1](LAB-UNKNOWN-OUTPUT-COERCION-P1-unknown-collection-output-gap-v0.md) | closed / proved — 36/36 PASS — HOLD / SAFETY-HIGH | `Collection[Unknown]` silently crosses typed output boundary in both Ruby and Rust TCs; root cause: `type_name()` shallow outer-name comparison — `Collection[Unknown]` and `Collection[T]` both return `"Collection"`, element type never compared; scalar `Unknown → T` IS caught by Ruby TC (OOF-TY0) but NOT `Collection[Unknown] → Collection[T]`; Rust TC silences scalar Unknown too (LAB-RACK-P9 guard); evidence: `rule_engine/engine.ig` `ExecuteRules` outputs `Collection[RuleDecision]` from `Collection[Unknown]` — both TCs emit no diagnostic; gap not call_contract-specific; no OOF code covers element-type mismatch at Collection output; AUTHORITY CLOSED: no TC changes / no dynamic dispatch proposal / no plugin model; SECTIONS: A scalar-caught 4/4 / B collection-gap 5/5 / C call_contract-source 4/4 / D engine.ig fixture 4/4 / E Ruby type_name anatomy 5/5 / F Rust parity 5/5 / G no-receipt 4/4 / H safety-closed 5/5; proof runner: `experiments/unknown_output_coercion_proof/verify_unknown_output_coercion_p1.rb`; card: `igniter-lab/.agents/work/cards/lab/LAB-UNKNOWN-OUTPUT-COERCION-P1.md` |
| LANG-STDLIB-TEXT-EQUALITY-P3 | closed / proved — 52/52 PASS | Ruby implementation of `==` in `operator_type`; ONE INSERTION: `when "=="` arm after `when "&&"` (line 1202) — 5 lines; LOGIC: `unknown?(left,right) || %w[Text String] cross-inclusion || same-type %w[Integer Bool]`; RETURN: `["stdlib.primitive.eq", type_ir("Bool")]` always — Bool returned even on error (Rust parity); G-04 verified via typed-declaration level (semantic_ir nil when errors present); DSA: dsa/sets `e == target` + dsa/strings `c.index == target_idx` both compile; FILTER: `filter(rows, p -> p.dir == "Debit")` predicate now Bool — OOF-COL3 clean, no Unknown bypass; SECTIONS: A regression 8/8 / B Text-String 6/6 / C Integer-Bool 4/4 / D Unknown 4/4 / E incompatible 6/6 / F Decimal 3/3 / G SIR 5/5 / H app-fixtures 8/8 / I filter 3/3 / J authority 5/5; P1 gap proof (`verify_text_equality_p1.rb`) reclassified — tests broken state, NOT regression suite; one file only: `typechecker.rb`; no emitter / no parser / no VM changes |
| LANG-STDLIB-TEXT-EQUALITY-P2 | implementation-planning-only — CLOSED / READY FOR P3 | Plans bounded Ruby implementation of `==` in `operator_type`; PLANNING DECISION: READY FOR P3 (no SPLIT / no HOLD); ONE FILE: `typechecker.rb` only; ONE INSERTION: `when "=="` arm after `when "&&"` (line 1202), before `else` (line 1203) — 5 lines; COMPATIBLE PAIRS: Text/Text, String/String, Text/String, String/Text, Integer/Integer, Bool/Bool + Unknown passthrough; Decimal excluded (BK-P02); LOGIC: `unknown?(left,right) \|\| %w[Text String].include? both \|\| same-type %w[Integer Bool].include?`; DIAGNOSTIC: OOF-TY0 "Type mismatch for ==: cannot compare X with Y" (custom message — no single expected type); RETURN: `["stdlib.primitive.eq", type_ir("Bool")]` always — Bool returned even on error (Rust parity); SIR: `stdlib.primitive.eq` — matches LAB-RACK-P6; no emitter / no parser / no VM / no `text_arg_compatible?` helper needed; FILTER SIDE-EFFECT: `filter(rows, p -> p.dir == "Debit")` predicate body now Bool — OOF-COL3 passes cleanly; PROOF MATRIX: 50 checks / 10 sections (A regression 8 / B Text-String 6 / C Integer-Bool 4 / D Unknown 4 / E incompatible 6 / F Decimal 3 / G SIR 5 / H app-fixtures 6 / I filter 3 / J authority 5); REGRESSIONS: string_core + collection_map_filter 61/61 + outcome 60/60 + sum 51/51 + fold 52/52 + typed_ref_p5 71/71; verify_text_equality_p1.rb is a GAP proof (tests broken state) — NOT in regression suite |
| [LANG-STDLIB-NUMERIC-COMPARISON-P1](LANG-STDLIB-NUMERIC-COMPARISON-P1-numeric-comparison-operators-v0.md) | closed / proved — 37/37 PASS | Readiness proof for `<`, `<=`, `>`, `>=` in Ruby and Rust TCs; `>` already works in both; `<` gap in Ruby only (Rust has it); `<=`/`>=` absent from both; parser parses all four (BINARY_OPS precedence 3); Integer-only in v0; Decimal HOLD; SIR names `stdlib.integer.{lt,lte,gt,gte}`; OOF-TY0 reuse; neural_net sigmoid (`x < (0-2500)`) + arch_patterns pipeline (`amount < 1`) blocked in Ruby / ok in Rust; inventory `stdlib.integer.gt` orphaned; lt/lte/gte absent; Rust SIR binary_op uses raw `op` not qualified name (secondary gap); next LANG-STDLIB-NUMERIC-COMPARISON-P2 implementation planning |
| [LAB-UNARY-MINUS-P1](LAB-UNARY-MINUS-P1-readiness-proof-v0.md) | closed / proved — 34/34 PASS | Readiness proof for parser unary minus; -500/-x/record/array/if-branch all fail at parse (Unexpected token in expression: op(-)); workaround 0-X confirmed ok; neural_net (network.ig weights + activations.ig threshold) + vector_math (Vec2Negate/Vec2Perp) app pressure documented; TC downstream gap proved (!x parses but infer_expr has no unary_op dispatch → OOF-TY0); implementation scope: parse_unary add minus case + infer_unary method for ! and - (two files only); no Decimal/Float/suffix/VM/Rust/emitter/assembler/stdlib changes |
| LANG-STDLIB-TEXT-EQUALITY-P1 | closed / readiness-proved — 46/46 PASS | `==` equality for Text/String/Integer/Bool in Ruby TC; GAP: `operator_type` has no `when "=="` arm — all `==` hit OOF-TY0 "Unsupported operator: =="; Rust `stdlib.primitive.eq` live since LAB-RACK-P6; COMPATIBLE PAIRS: Text/Text, String/String, Text/String, String/Text, Integer/Integer, Bool/Bool + Unknown passthrough; Decimal excluded (BK-P02); SIR: `stdlib.primitive.eq`; no VM / no parser / no emitter changes; normalization: byte/value-exact, no locale; ordering orthogonal (== ≠ <); APP EVIDENCE: vector_editor active_tool/layer_id + decision_tree node_id/kind_tag + arch_patterns event_kind/state_machine balance; 8 sections / 46 checks; proof: `experiments/text_equality_proof/verify_text_equality_p1.rb`; planning: `LANG-STDLIB-TEXT-EQUALITY-P1-readiness-proof-v0.md`; next: P2 implementation planning — one `when "=="` arm in `operator_type` ~10 lines |
| [LANG-STDLIB-SUM](LANG-STDLIB-SUM-field-projection-sum-v0.md) / [P2](LANG-STDLIB-SUM-P2-implementation-planning-v0.md) / P3 | authored + implementation-planning-only + closed/proved 51/51 PASS | `stdlib.collection.sum` two-arg field-projection form only: `sum(Collection[T], Symbol) -> F` where F = declared type of Symbol-named field in T; one-arg form BLOCKED (absent from stdlib spec; Rust scale-stripping bug; requires LAB-STDLIB-NUMERIC-P1); return type is structural field lookup — scale-preserving for Decimal[N] without arithmetic type inference; bare source alias `sum` accepted; non-Symbol second arg → OOF-COL5; missing field → OOF-COL5; OOF-COL1 arity / OOF-COL2 non-Collection first arg; empty-collection runtime value deferred to P4+; sumBy explicitly deferred; fold-derivation noted (independent implementation); Ruby TC: OOF-TY0 for both forms; P2 PLANNING: READY FOR P3 — one file (typechecker.rb only); TWO INSERTIONS: (1) `when "sum"` dispatch arm in infer_call after COLLECTION_HOF_FNS arm; (2) `infer_sum_call` private method (~45 lines) after infer_collection_hof_call; SEPARATE from COLLECTION_HOF_FNS — Symbol not lambda, no has_symbol flag, direct dispatch matching or_else/outcome precedent; @type_shapes field lookup: element_type_from_collection → type_shapes[elem_type_name][field_name] → declared type_ir (Decimal[N] params preserved automatically); non-Symbol → OOF-COL5; missing field + non-Unknown elem → OOF-COL5; Unknown elem type → Unknown permissive (no error); no numeric constraint in v0 (non-numeric field type accepted, deferred to P4+); SIR fn = "stdlib.collection.sum" (qualified — closes Rust bare-"sum" parity gap); zero emitter changes; regressions: string_core + typed_ref_p5 71/71 + collection HOF 61/61 + outcome 60/60; proof matrix 8 sections / 51 checks; grounded by LAB-STDLIB-SUM-P1 46/46 PASS (SPLIT-NUMERIC Split A) + stdlib/collections.ig spec + stdlib_extension.ig conformance fixture; P3 CLOSED: Ruby TC implementation proved 51/51 PASS — when "sum" arm + infer_sum_call; Decimal[2] params [2] integer array; fold dispatches through COLLECTION_HOF_FNS unaffected; next LAB-STDLIB-NUMERIC-P1 (gates one-arg sum Split B) / Rust SIR parity card |
| [LANG-STDLIB-FOLD](LANG-STDLIB-FOLD-collection-fold-v0.md) | authored-pending-review | `stdlib.collection.fold` v0: 1 entry contract; `Collection[T] × Acc × ((Acc,T)→Acc) → Acc`; Acc from seed literal type_tag (fold_stream_result_type pattern); inline lambda only (2 params: acc+elem); lambda return type must equal Acc (OOF-COL4 strict); result type = Acc (scalar); fold_stream T3 path unaffected; OOF-COL4 for all fold errors; OOF-COL5 reserved for sum; pure/core/authority_surface:none; semantic_ir_name==canonical_name; no legacy_sir; grounded by LAB-STDLIB-FOLD-P1 50/50 PASS (ACCEPT); next LANG-STDLIB-FOLD-PROP-P2 implementation planning |
| [LANG-STDLIB-FOLD-P2](LANG-STDLIB-FOLD-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans bounded Ruby canon implementation of `stdlib.collection.fold` in TypeChecker only; PLANNING DECISION: READY FOR P3 (no SPLIT / no HOLD); ONE FILE: `typechecker.rb` only; TWO INSERTIONS: (1) `when "fold"` dispatch arm after `COLLECTION_HOF_FNS` arm in `infer_call`; (2) `infer_fold_call` private method after `infer_lambda_body` before `infer_or_else` (~55 lines); FOLD NOT IN COLLECTION_HOF_FNS: structural incompatibility — 3 args / 2-param lambda / Acc scalar result; SEED TYPE: `infer_expr(args[1], ...)` — handles literals/refs/calls (better than fold_stream literal-only pattern); 2-PARAM BINDING: `symbol_types.merge(params[0] => acc_type, params[1] => elem_type)`; LAMBDA BODY: `infer_lambda_body` unchanged — block-body ERP fixture already works (proven in P3 map/filter); OOF-COL4: arity/non-Collection/non-lambda/param-count (early-return) + return-mismatch (non-early, acc_type returned); Unknown permissive; SIR: `"stdlib.collection.fold"` inline — zero emitter changes; fold_stream T3 path unaffected; proof matrix ≥52 checks / 8 sections (A regression / B basics / C lambda inference / D OOF-COL4 / E SIR names / F app fixtures / G fold_stream coexistence / H authority); proof runner: `verify_stdlib_fold_p3.rb`; ~58 new lines; next: LANG-STDLIB-FOLD-PROP-P3 Ruby implementation proof |
| LANG-STDLIB-FOLD-P3 | closed / proved — 52/52 PASS | Bounded Ruby canon implementation of `stdlib.collection.fold`; `when "fold"` dispatch arm in `infer_call` after `when "sum"` + `infer_fold_call` private method after `infer_lambda_body`; FOLD NOT IN COLLECTION_HOF_FNS (3 args / 2-param lambda / Acc scalar result); SEED TYPE: `infer_expr(args[1])` — handles literals/refs/calls; 2-PARAM BINDING: `symbol_types.merge(params[0] => acc_type, params[1] => elem_type)`; LAMBDA BODY: `infer_lambda_body` unchanged; OOF-COL4 (Unknown-permissive): arity/non-Collection/non-lambda/param-count (early-return) + return-mismatch (non-early); SIR `fn = "stdlib.collection.fold"` (zero emitter changes); fold_stream T3 path unaffected; TC arithmetic gap noted: `+` always returns Integer — bookkeeping body `acc + 0.00` gives OOF-COL4 (dispatch proved via F-03 positive evidence); map/filter runner updated (H-01/H-02/G-03 adapted for fold+sum dispatch); regressions: collection_map_filter_p3 61/61 + stdlib_outcome_p3 60/60 + string_core PASS; 8 sections A–H; no inventory / no Rust / no VM / no fold_stream / no OOF beyond OOF-COL4; next LANG-STDLIB-FOLD-PROP-P4 inventory amendment + Rust parity |
| [LANG-FOLD-STRUCT-ACCUMULATOR-P1](LANG-FOLD-STRUCT-ACCUMULATOR-P1-readiness-v0.md) | closed / readiness proved — 62/62 PASS | Record/struct accumulator readiness for `fold(Collection[T], Acc, (Acc,T)->Acc)`; verdict: ROUTE P2 implementation planning, no direct implementation; Ruby TC already supports record Acc with parenthesized record-returning lambda `-> ({ ... })`, including inline/named/annotated seed and `call_contract` returning Portfolio; Ruby catches bad record field in lambda body with OOF-TY0; unparenthesized `-> { sum: ... }` is a block/ergonomics issue, not an Acc ban; Rust scalar fold OK, annotated record seed OK, but inline record seed remains Unknown and fails `OOF-TY1 expected Stats, got Unknown`; Rust fold lambda return validation gap: bad string/bad record field currently compile cleanly; Ruby emitter has no fold-specific pipeline lowering while Rust emitter already has fold pipeline node, so P2 must cover Rust TC plus Ruby/Rust lowering parity; trade_robot evidence: RunBacktest manual `p0..p10` Portfolio threading and RSI fold-state comments; boundaries: no group_by/join/flat_map/scan/temporal/app migration/runtime authority; proof: `experiments/fold_struct_accumulator_proof/verify_fold_struct_accumulator_p1.rb`; card: `.agents/work/cards/lang/LANG-FOLD-STRUCT-ACCUMULATOR-P1.md` |
| [LANG-STDLIB-IS-EMPTY-PROP-P4](LANG-STDLIB-IS-EMPTY-PROP-P4-rust-parity-proof-v0.md) | closed / proved — 50/50 PASS | Rust parity for `stdlib.collection.is_empty` + `stdlib.collection.non_empty`; `"is_empty" | "non_empty"` arm in `typechecker.rs` (OOF-COL1/COL2 + Unknown permissive; Bool on all paths); `COLLECTION_HOF_OPS` + `matches!` guard in `emitter.rs` for canonical SIR names; inventory `lowering_status` upgraded to `dual-toolchain`; 50/50 PASS; 10 sections A–J (regression/is_empty/non_empty/COL1/COL2/unknown/SIR/source/inventory/authority); no parser/Ruby TC/assembler/VM/unary_op changes |
| [LANG-STDLIB-IS-EMPTY](LANG-STDLIB-IS-EMPTY-collection-is-empty-non-empty-v0.md) / [P2](LANG-STDLIB-IS-EMPTY-PROP-P2-implementation-planning-v0.md) / P3 | authored + implementation-planning-only + closed/proved 60/60 PASS | `stdlib.collection.is_empty` + `stdlib.collection.non_empty` v0: 2 entry contracts; both `Collection[T] → Bool`; pure/total/core/authority_surface:none; semantic_ir_name==canonical_name; bare source aliases (`is_empty` / `non_empty`); NO new OOF codes — OOF-COL1 arity / OOF-COL2 non-Collection first arg reused from count; OOF-COL6 already claimed by append; NON-DERIVABILITY: `!is_empty(x)` → OOF-TY0 "Unsupported expression kind: unary_op" — `!` parsed by parser (`parse_unary` → `unary_op` node) but absent from `infer_expr` case dispatch; `non_empty` is first-class sibling not alias; APP PRESSURE: 4 fixtures / 7 comments — dsa/sets.ig x2 + dsa/graphs.ig + arch_patterns/state_machine.ig + bloom_filter/ops.ig; DISPATCH: `when "is_empty", "non_empty"` arm after `when "append"` + shared `infer_is_empty_call` ~22 lines — NOT in COLLECTION_HOF_FNS (Bool result diverges); Bool returned on ALL paths including OOF error paths; SIR fn = qualified canonical names; P3 CLOSED: Ruby TC proved 60/60 PASS — two insertions in `typechecker.rb` + 2 inventory entries (27→29 entries) + digest `a94ef6fe…→ebf9ad74…`; regressions: map/filter/count/fold/sum/append all clean; grounded by LAB-STDLIB-IS-EMPTY-P1 48/48 PASS; next LANG-STDLIB-IS-EMPTY-PROP-P4 Rust parity |
| [LANG-STDLIB-COLLECTION-MAP-FILTER-P2](LANG-STDLIB-COLLECTION-MAP-FILTER-P2-implementation-planning-v0.md) | implementation-planning-only — READY FOR P3 | Plans bounded Ruby canon implementation of `stdlib.collection.map`, `stdlib.collection.filter`, `stdlib.collection.count` in TypeChecker only; PLANNING DECISION: READY FOR P3 (all three in one slice; no SPLIT); authorized file: `typechecker.rb` only; THREE INSERTIONS: (1) `COLLECTION_HOF_FNS` constant after `OUTCOME_STDLIB_FNS` ({map: arity 2/has_lambda true, filter: arity 2/has_lambda true, count: arity 1/has_lambda false}); (2) `when *COLLECTION_HOF_FNS.keys` arm in `infer_call` after OUTCOME arm; (3) `infer_collection_hof_call` + `infer_lambda_body` private methods (~80-90 lines); LAMBDA BINDING: `element_type_from_collection` extracts T → `symbol_types.merge(param => elem_type)` → `infer_expr(body, local_symbols)`; RETURN TYPES: map=`collection_type_ir_from(body_type)` / filter=`collection_type_ir_from(elem_type)` passthrough / count=Integer; SIR qualified names automatic (generic `semantic_expr` preserves fn field, zero emitter changes); COUNT T3 COEXISTENCE: no conflict — T3 path goes through `handle_t3_variant`, never reaches `infer_call`; OOF-COL1 (arity/non-lambda) / OOF-COL2 (non-Collection first arg) active; OOF-COL3 (filter non-Bool predicate) P3 implementation decision; REGRESSION: string_core + typed_ref_p5 + stdlib_outcome_p3; PROOF: ≥56 checks / 8 sections (regression 7 / count 8 / filter 8 / map 8 / SIR names 6 / type inference 7 / app fixtures 6 / authority 6); proof runner: `experiments/stdlib_collection_proof/verify_stdlib_collection_map_filter_p3.rb`; no inventory edits in P3; next: LANG-STDLIB-COLLECTION-MAP-FILTER-PROP-P3 Ruby implementation proof |
| [LANG-STDLIB-COLLECTION-MAP-FILTER-P3](LANG-STDLIB-COLLECTION-MAP-FILTER-P3-implementation-proof-v0.md) | closed / proved — 61/61 PASS | Bounded Ruby canon implementation of `stdlib.collection.map`, `stdlib.collection.filter`, `stdlib.collection.count` in TypeChecker; `COLLECTION_HOF_FNS` constant (3 entries after `OUTCOME_STDLIB_FNS`) + `when *COLLECTION_HOF_FNS.keys` dispatch arm in `infer_call` + `infer_collection_hof_call` + `infer_lambda_body` private methods; OOF-COL1 arity/non-lambda / OOF-COL2 non-Collection first arg / OOF-COL3 filter non-Bool predicate active; SIR `fn` = qualified canonical name via zero-change generic `semantic_expr`; map→`collection_type_ir_from(body_type)` / filter→`collection_type_ir_from(elem_type)` passthrough / count→Integer; element type bound via `element_type_from_collection` + `symbol_types.dup`; `infer_lambda_body` handles single-expr and block-form bodies; T3 count path unaffected; pre-existing `==` operator gap (OOF-TY0) noted — Unknown passes OOF-COL3 permissively; regressions: string_core_proof PASS + stdlib_outcome_p3 60/60 + typed_contract_ref_p5 71/71; 8 sections A–H (regression 7 / count 8 / filter 9 / map 9 / SIR names 7 / type inference 8 / app fixtures 6 / authority 7); no inventory edits / no Rust / no VM / no fold / no sum / no predicate-count; next LANG-STDLIB-COLLECTION-MAP-FILTER-PROP-P4 Rust parity / canonical SIR names |
| [LANG-STDLIB-COLLECTION-MAP-FILTER-P4](../../../igniter-lab/lab-docs/lang/lab-stdlib-collection-map-filter-count-rust-parity-v0.md) | closed / proved — 67/67 PASS | Bounded Rust parity implementation for `stdlib.collection.map`, `stdlib.collection.filter`, `stdlib.collection.count`; THREE GAPS CLOSED: (1) SIR fn names bare→canonical `stdlib.collection.*` via `COLLECTION_HOF_OPS` const in `semantic_expr` + delegation in `semantic_expr_for_compute` (key architectural discovery: compute path must explicitly delegate to reach `semantic_expr` rewrite); (2) map lambda param binding hardcoded `Integer`→element type T via `get_param(&first_arg_type, 0)`; (3) filter predicate validation: lambda bound to elem T + OOF-COL3 for non-Bool/non-Unknown predicate body (pushed to outer `type_errors` via `temp_errors` pattern); TWO FILES: `typechecker.rs` (~55 new lines) + `emitter.rs` (~20 new lines); map result `Collection[T]→Collection[U]` wraps lambda body type / filter passthrough preserved / count→Integer unchanged; `==` predicate Unknown passthrough gap pre-existing / OOF-COL1/COL2 Rust parity deferred to P5; T3 count path unaffected; no Ruby changes / no fold / no sum / no VM / no inventory edits; 9 sections A–I (regression 7 / count 8 / filter 9 / map 9 / SIR names 7 / type inference 8 / app fixtures 6 / lambda binding 6 / authority closed 7); next LANG-STDLIB-COLLECTION-MAP-FILTER-PROP-P5 stdlib-inventory.json integration + OOF-COL1/COL2 Rust parity |
| LANG-STDLIB-COLLECTION-MAP-FILTER-P5 | closed / proved — 86/86 PASS | Inventory integration + OOF-COL1/COL2 Rust parity for `stdlib.collection.map`, `stdlib.collection.filter`, `stdlib.collection.count`; TWO NEW INVENTORY ENTRIES: map (type_params T/U / output Collection[U] / diagnostics OOF-COL1+OOF-COL2 / dual-toolchain / lab-implemented) + filter (type_params T / output Collection[T] / diagnostics OOF-COL1+OOF-COL2+OOF-COL3 / dual-toolchain / lab-implemented); UPDATED count proof_lineage to include P3+P4+T3 path note; stdlib_surface_digest recomputed (34671fb37f3ba302e906148aa433c313da1968a0cd9d019f8f4a5b4ac92136c4); THREE OOF-COL1/COL2 ARMS: count arm (args.len()!=1 → OOF-COL1; non-Collection → OOF-COL2) + filter arm (guarded fn_name=="filter"; args.len()!=2 → OOF-COL1; non-Collection → OOF-COL2) + map arm (args.len()!=2 → OOF-COL1; non-Collection → OOF-COL2); no Ruby changes / no fold/sum / no VM / no new import authority; 10 sections A–J (schema 10 / digest 8 / map entry 9 / filter entry 9 / count update 7 / bidirectional 8 / OOF-COL1 10 / OOF-COL2 10 / P4 regression 8 / authority 7); map/filter/count stabilization track CLOSED |
| [LANG-PARSER-CONTEXTUAL-KEYWORDS-P1](LANG-PARSER-CONTEXTUAL-KEYWORDS-P1-contextual-keyword-parity-v0.md) / P2 | P1 planning-complete; P2 closed / proved — 61/61 PASS | Ruby parser contextual keyword parity; 8 call-site changes in `parser.rb`: `parse_input_decl:950` / `parse_output_decl:957` / `parse_compute_decl:1031` / `parse_params:1358` / `parse_let_stmt:1388` / `parse_lambda:1816` (multi-param) — `name_token!(%i[ident])` → `name_token!(%i[ident keyword])`; `parse_lambda:1821` (single-param) + `parse_call_arg:1781` (lambda dispatch) — added `|| peek_type?(:keyword)`; CORPUS 8/8: `label`/`message`/`from`/`match`/`profile`/`authority`/`lead`/`step` accepted in all binding positions; working positions (type fields/record keys/dotted access) unchanged; DT-P02 `input label : String` unblocked; Rust already permissive (no Rust changes); KEYWORDS constant and typechecker unaffected; arc CLOSED — no P3 |
| LANG-UNARY-OPERATORS-P3 | closed / proved — 54/54 PASS | Bounded Ruby implementation of unary `!` and unary `-`; THREE FILE CHANGES: (1) `parser.rb` `parse_unary` adds `:op "-"` branch after `:bang` — operand=`parse_postfix` (no chaining); (2) `typechecker.rb` adds `when "unary_op"` arm before `else` fallthrough + `infer_unary_op` private method (~26 lines) — `!`→`stdlib.primitive.not`/Bool, `-`→`stdlib.integer.neg`/Integer, Bool/Integer returned on all paths incl. error, Unknown permissive, TC converts `unary_op`→`call` (no unary_op in SIR); (3) `semanticir_emitter.rb` adds `when "unary_op"` arm + `lower_unary`/`unary_operator_for` (~30 lines); TWO INVENTORY ENTRIES: `stdlib.primitive.not` (primitive/Bool→Bool/ruby-only) + `stdlib.integer.neg` (numeric/Integer→Integer/ruby-only); digest recomputed (0bece8e2…); `!is_empty(items)` composition works (H-04); `0-X` workaround still compiles; record literal `{a:-300}` works; regressions: binary/equality/collection all clean; no Decimal/Float/Rust/VM/assembler/app-source changes; 54/54 PASS / 10 sections A–J |
| [LANG-UNARY-OPERATORS](LANG-UNARY-OPERATORS-unary-operators-v0.md) | authored-pending-review | Unary operators `!` and `-`; `!: Bool → Bool` SIR `stdlib.primitive.not`; `-: Integer → Integer` SIR `stdlib.integer.neg`; Decimal/Float deferred; OOF-TY0 for wrong operand (no new codes); Unknown permissive on both; parser forms: literal/variable/record-field/array-literal/if-branch/parenthesized; Ruby `-` token is `:op`+value not `:minus`; CURRENT GAP: both parsers handle `!` (`:bang`) but not `-`; both TCs have no `unary_op` arm in `infer_expr` — OOF-TY0 "Unsupported expression kind: unary_op"; Rust runtime confirmed `!x` where x:Bool → OOF-TY0; app pressure: neural_net (6+ `0-X` weight workarounds) + vector_math (Vec2Negate/Vec2Perp `0-v.x` workarounds); evidence: LAB-UNARY-MINUS-P1 34/34 PASS; no parser/TC/emitter/inventory/VM changes in P1; next LANG-UNARY-OPERATORS-P2 implementation planning |
| LANG-EMITTER-ENCODING-P1 | closed / proved — 30/30 PASS | Readiness proof for Ruby emitter JSON UTF-8 crash; CRASH PATH: `multifile_resolver.rb:96` `path.read` (no encoding) → US-ASCII string → `multifile_resolver.rb:317` `JSON.generate(canonical_json(material))` → `JSON::GeneratorError: "\xE2" on US-ASCII`; FAILING LAYER: JSON serialization (not lexer/parser/SHA256); single-file path safe (raw source excluded from ParsedProgram.to_h); FIX: `encoding: "utf-8"` at 2 core production sites + 4 CLI/experimental sites; Rust immune by design (fs::read_to_string always UTF-8); json 2.x warns on ASCII-8BIT / raises on US-ASCII; CLASSIFICATION: correctness — locale-dependent crash on valid .ig input; 7 sections A–G / 30 checks; proof: `experiments/emitter_encoding_proof/verify_emitter_encoding_p1.rb`; card: `igniter-lab/.agents/work/cards/lang/LANG-EMITTER-ENCODING-P1.md`; next LANG-EMITTER-ENCODING-P2 (6-site fix + regression) |
| [LANG-STDLIB-COLLECTION-CONCAT-PROP-P4](LANG-STDLIB-COLLECTION-CONCAT-PROP-P4-rust-parity-proof-v0.md) | closed / proved — 32/32 PASS | Rust parity for `stdlib.collection.concat`; THREE CHANGES: (1) `"concat"` arm in `typechecker.rs` replaced with OOF-COL1/COL2/COL7 parity (Unknown/Collection→collection path; else→text path; OOF-COL1 arity; OOF-COL2 second non-Collection; OOF-COL7 elem mismatch non-early-return; result `Collection[T]`); (2) `rewrite_concat_calls` DSA-P03 fix — `first_type == "Unknown"` routes to `stdlib.collection.concat` (field-access like `s.elements` returns Unknown from `quick_arg_type`); (3) emitter element-type erasure fix — inject `decl.type_info` as `resolved_type` on call JSON so fallback `Collection{params:[]}` is skipped; inventory `lowering_status` → `dual-toolchain`; 9 sections A–I / 32 checks; proof runner: `igniter-lab/igniter-compiler/verify_stdlib_collection_concat_p4.rb` |
| LANG-STDLIB-COLLECTION-CONCAT-PROP-P3 | closed / proved — 67/67 PASS | Ruby TC implementation of `stdlib.collection.concat`; `when "concat"` dispatch arm BEFORE `when *TEXT_STDLIB_FNS.keys` in `infer_call`; `infer_concat_call` ~60 lines — routing by first-arg resolved_type (Collection/Unknown → collection path; other → `infer_text_call` delegation); OOF-COL1 arity / OOF-COL2 second-arg non-Collection / **OOF-COL7 FIRST ACTIVATION** element-type mismatch; Unknown permissive on routing + both element positions; result type = Collection[elem1] (falls back to elem2 if elem1=Unknown); inventory updated: lifecycle=lab-implemented/lowering=ruby-only/aliases=[source_alias=concat]/diagnostics=[COL1,COL2,COL7]/digest=9163d103…; TEXT_STDLIB_FNS retains concat for delegation; text.concat regression clean (C-01..C-07); DSA inline SetInsert equiv typecheck=ok; pre-existing compiler encoding limitation with DSA types.ig Unicode comments documented; 10 sections A–J / 67 checks |
| [LANG-STDLIB-COLLECTION-CONCAT-PROP-P2](LANG-STDLIB-COLLECTION-CONCAT-PROP-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans bounded Ruby TC implementation of `stdlib.collection.concat`; disambiguation by first-arg resolved_type (Collection/Unknown→collection path; else→delegate to `infer_text_call`); `when "concat"` arm before `when *TEXT_STDLIB_FNS.keys` in `infer_call`; `infer_concat_call` ~48 lines after `infer_is_empty_call`; OOF-COL1 arity / OOF-COL2 non-Collection second arg (early-return) / OOF-COL7 element type mismatch (non-early-return, first activation in P3); Unknown permissive on both positions + element type; Rust P4: `quick_arg_type` FieldAccess one-level lookup (DSA-P03 fix) + emitter params=[T] not params=[] (Bug 2 partial fix — accepted limitation: bare-ref elem type falls back to Unknown); inventory P3: lifecycle=lab-implemented/lowering=ruby-only/source_alias=concat/diagnostics=[COL1,COL2,COL7]; proof matrix: P3 ≥65 checks/10 sections (A source/B COL1/C COL2/D COL7/E Unknown/F happy/G text regression/H DSA fixture/I inventory/J authority) + P4 ≥50 checks/8 sections; authorized files: P3=`typechecker.rb`+inventory / P4=`typechecker.rs`+`emitter.rs`; no VM/flatten/flat_map/join/group_by |
| [LANG-STDLIB-COLLECTION-CONCAT](LANG-STDLIB-COLLECTION-CONCAT-collection-concat-v0.md) | authored-pending-review | `stdlib.collection.concat` v0: `Collection[T] × Collection[T] → Collection[T]`; source alias `concat` shared with `stdlib.text.concat` — disambiguated by first arg type at TC time (Collection/Unknown → collection path; Text/other → text path); OOF-COL1 arity / OOF-COL2 non-Collection first arg (reuse) / **OOF-COL7 NEW** element type mismatch between two collections; Unknown permissive on both element types; DSA-P03 root cause documented: `rewrite_concat_calls` `quick_arg_type` returns Unknown for field access → text.concat mislabeling (silent wrong SIR, no diagnostic); element type parameter erased in Rust emitter (params=[]); Ruby TC: all collection concat → OOF-TY0 via text path (gap); Rust TC: bare-ref first arg → ok/collection.concat; field-access → DSA-P03 (mislabeled); bootstrap `call_contract("concat",...)` unaffected; inventory entry: orphaned/single-toolchain/no source_alias → P3 upgrades to lab-implemented/ruby-only + adds source_alias; P4 upgrades to dual-toolchain after DSA-P03 fix + element type erasure fix; no flatten/flat_map/join/group_by; readiness proof: 45/45 PASS |
| [LANG-STDLIB-COLLECTION-MAP-FILTER](LANG-STDLIB-COLLECTION-MAP-FILTER-collection-map-filter-count-v0.md) | authored-pending-review | `stdlib.collection.map` / `stdlib.collection.filter` / `stdlib.collection.count` v0: 3 entry contracts; all pure/core/authority_surface:none; semantic_ir_name==canonical_name; bare source aliases (map/filter/count) accepted; inline lambda only (no Fn[T,U] type); element_type_from_collection primitive reused (typechecker.rb ~line 1825); count-all-elements-only in v0 (predicate form deferred); Ruby TC: zero HOF dispatch — map/filter OOF-TY0, count OOF-TY0 for regular calls (T3-only gap); Rust TC: all three dispatched, map lambda param Integer gap + bare SIR name gap documented; OOF-COL1..COL5 namespace reserved (COL1 arity / COL2 non-Collection first arg active in P2); grounded by LAB-STDLIB-COLLECTION-P1 64/64 PASS (SPLIT verdict Group A); next LANG-STDLIB-COLLECTION-MAP-FILTER-PROP-P2 implementation planning |
| [LANG-STDLIB-COLLECTION-APPEND](LANG-STDLIB-COLLECTION-APPEND-collection-append-v0.md) | authored-pending-review | `stdlib.collection.append` v0: single-item append `Collection[T] × T → Collection[T]`; pure/core/authority_surface:none; semantic_ir_name==canonical_name; bare alias `append`; order-preserving (item at end); OOF-COL1 arity / OOF-COL2 non-Collection first arg / OOF-COL6 (NEW) item type concrete mismatch; bootstrap pattern (T×T via call_contract) outside canonical type — stays via call_contract until stdlib.collection.empty; DISTINCT from stdlib.collection.concat (orphaned, Collection×Collection); 6+ app fixture evidence surfaces (vector_editor/decision_tree/igniter_parser/bloom_filter/arch_patterns); all current usage via call_contract("append",...); `import stdlib.collection.{ append }` already declared in fixtures — normative; lifecycle=proof-local / lowering=none / stability=experiment-pass; next: LANG-STDLIB-COLLECTION-APPEND-PROP-P2 Ruby TC implementation + `when "append"` arm + `infer_append_call` (~40 lines) + proof runner ≥50 checks |
| [LANG-STDLIB-COLLECTION-APPEND-P4](../../../igniter-lab/lab-docs/lang/lab-stdlib-collection-append-rust-parity-p4-v0.md) | closed / proved — 66/66 PASS | Rust parity for `stdlib.collection.append`; `"append"` arm in `typechecker.rs` (OOF-COL1/COL2/COL6 + Unknown permissive); `("append", "stdlib.collection.append")` in `COLLECTION_HOF_OPS` + `"append"` in `TEXT_STDLIB_OPS_C` delegation guard in `emitter.rs`; inventory `lowering_status` upgraded to `dual-toolchain`; stdlib_surface_digest recomputed (a94ef6fe…); G section proves Ruby P3 parity (6 checks); 66 checks / 10 sections A–J; no parser/VM/capability/app fixture changes |
| [LANG-STDLIB-COLLECTION-APPEND-P3](../../../igniter-lab/lab-docs/lang/lab-stdlib-collection-append-ruby-p3-v0.md) | closed / proved — 65/65 PASS | Ruby TC implementation of `stdlib.collection.append`; TWO INSERTIONS in `typechecker.rb`: (1) `when "append"` arm after `when "fold"`; (2) `def infer_append_call` ~38 lines — OOF-COL1 arity / OOF-COL2 non-Collection / OOF-COL6 concrete mismatch (FIRST ACTIVATION) / Unknown permissive; SIR fn = `"stdlib.collection.append"` qualified inline (zero emitter changes); bootstrap `call_contract("append",...)` unaffected (fn="call_contract" never hits append arm); ONE NEW INVENTORY ENTRY: lifecycle=lab-implemented / lowering=ruby-only / type_params=[T] / input=[Collection[T],T] / output=Collection[T] / diagnostics=[COL1,COL2,COL6]; entry count 26→27; stdlib_surface_digest recomputed (f0e592fc5c6781dff24cf5a590b7938f21b80bdb0dc5c6aceb1b907cc5391a75); no emitter/parser/Rust TC/VM/app fixture changes; 65 checks / 10 sections (A source 8 / B COL1 6 / C COL2 6 / D COL6 6 / E Unknown 5 / F happy 8 / G bootstrap 4 / H inventory 8 / I authority 6 / J regression 8); next LANG-STDLIB-COLLECTION-APPEND-PROP-P4 Rust parity |
| [LANG-STDLIB-COLLECTION-APPEND-P2](LANG-STDLIB-COLLECTION-APPEND-PROP-P2-implementation-planning-v0.md) | implementation-planning-only — CLOSED / READY FOR P3 | Plans bounded Ruby TC implementation of `stdlib.collection.append`; PLANNING DECISION: READY FOR P3 (no SPLIT / no HOLD); TWO INSERTIONS in `typechecker.rb`: (1) `when "append"` arm after `when "fold"` / before `when "or_else"` (~line 897); (2) `def infer_append_call` after `infer_fold_call` (~line 2524) — ~38 lines; NOT IN COLLECTION_HOF_FNS: append takes an item not a lambda — sum/fold separate-arm precedent; ELEMENT TYPE: `element_type_from_collection(collection_arg.fetch("resolved_type"))` — existing helper; RETURN TYPE: `collection_type_ir_from(elem_type)` — preserves input element type; OOF-COL1: `args.length != 2` → early-return; OOF-COL2: non-Collection/non-Unknown first arg → early-return; OOF-COL6 (FIRST ACTIVATION): both concrete and different → non-early-return; Unknown permissive on both sides; SIR fn = `"stdlib.collection.append"` direct — zero emitter changes for Ruby P3; BOOTSTRAP: `call_contract("append",...)` bypasses infer_call — unaffected; INVENTORY TIMING: P3 deliverable (lifecycle=lab-implemented / lowering=ruby-only / aliases=[{source_alias, "append"}] / diagnostics=[COL1,COL2,COL6]) — activates importability once import surface P3 live; digest recomputed with Ruby canonical_json; AUTHORIZED FILES: typechecker.rb + stdlib-inventory.json + proof runner; RUST P4 SCOPE: `"append"` arm in typechecker.rs + COLLECTION_HOF_OPS entry + TEXT_STDLIB_OPS_C delegation guard + inventory lowering upgrade to dual-toolchain; PROOF MATRIX: 65 checks / 10 sections (A source 8 / B OOF-COL1 6 / C OOF-COL2 6 / D OOF-COL6 6 / E Unknown permissive 5 / F happy path 8 / G bootstrap 4 / H inventory 8 / I authority 6 / J regression 8); no emitter/parser/Rust TC/VM/runtime/app fixture/concat/fold/sum changes |
| [LANG-RUBY-RECORD-LITERAL-INFERENCE-P5](LANG-RUBY-RECORD-LITERAL-INFERENCE-P5-empty-array-field-proof-v0.md) | closed / direct implementation — 29/29 PASS | Empty array field covariance in `infer_record_literal` structural matching; P4 readiness diagnosed SIM-P14: `structurally_assignable?(Collection[Unknown], Collection[T])` returns false (correct for output boundary) but rejects `[]` → `Collection[Unknown]` fields during record candidate matching; FIX: new `empty_collection_assignable?` helper (outer-name == "Collection" + all actual params Unknown + arity match) + or-clause in candidate field check; `structurally_assignable?` body UNCHANGED — output boundary strictness preserved; sim_framework `RunEcosystemSim.initial_state` now infers as `SimState`; SIM-P14 RESOLVED; SIM-P10/P11 pre-existing (String/Text, unrelated); SECTIONS A-F 29/29; P3 regression 76/76 PASS; ONE FILE: `typechecker.rb` +14 lines; no parser / no Rust / no emitter / no new OOF codes; proof: `experiments/record_literal_inference_proof/verify_record_literal_inference_p4.rb`; card: `.agents/work/cards/lang/LANG-RUBY-RECORD-LITERAL-INFERENCE-P5.md` |
| [LAB-NESTED-RECORD-LITERAL-TYPING-P1](LAB-NESTED-RECORD-LITERAL-TYPING-P1-nested-record-hint-leakage-v0.md) | closed / direct implementation — 60/60 PASS | Nested record literal hint leakage fix in Ruby `infer_record_literal`; root cause: field-value expressions reused the outer `node_name`, so `output result : Mat3` caused inner Vec3 row literals `{x,y,z}` to validate as Mat3 and emit false `missing r0/r1/r2` / `unexpected x/y/z`; FIX: synthetic field-node context (`result.r0`) plus temporary expected-field record hint under that synthetic node, restored with `ensure`; direct nested Mat3 and two-level Scene fixtures clean; inner bad fields still emit precise OOF-TY0 on `result.r0`; ordinary outer missing/extra fields remain on `result`; same-name output collision and annotated compute hints preserved; P3 structural proof 76/76 and P5 empty-array proof 29/29 still pass; ONE FILE: `typechecker.rb`; no parser / no emitter / no Rust / no app migration / no output assignability relaxation / no new OOF codes; proof: `experiments/nested_record_literal_typing_proof/verify_nested_record_literal_typing_p1.rb`; source card: `igniter-lab/.agents/work/cards/lang/LAB-NESTED-RECORD-LITERAL-TYPING-P1.md` |
| [LANG-RUBY-RECORD-LITERAL-INFERENCE-P3](LANG-RUBY-RECORD-LITERAL-INFERENCE-P3-structural-proof-v0.md) | closed / direct implementation — 76/76 PASS | Structural field-set matching fallback in `infer_record_literal` for unannotated intermediate record literals; ONE METHOD: `infer_record_literal` +14 lines after hint-path block; ALGORITHM: sort literal field names → `@type_shapes.select` entries with exact sorted field-name equality + `structurally_assignable?(act_type, exp_type)` per field (bare-Unknown permissive: `type_name(act_type) == "Unknown"` short-circuits); WHY `structurally_assignable?` NOT `type_name`: DSA has `ArrayIndexed { size, elements: Collection[IndexedElement] }` and `IntSet { size, elements: Collection[Integer] }` — same field names; `type_name` shallow sees both `"Collection"` and fires OOF-TY0; `structurally_assignable?` recurses into params and correctly disambiguates; CANDIDATE RULES: exact sorted field-name set / structurally_assignable? per field / Unknown permissive override / unique→infer type_ir / zero→Unknown no error / multiple→OOF-TY0 "Ambiguous record literal type: fields {…} match A, B"; P2 INTERACTION: P2 temp hint causes early return before structural fallback — P3 never reached for annotated computes; `@output_type_hints` NOT mutated; APP IMPACT: 3 apps CLEAN dsa/dataframes/neural_net; vector_math 41→36 (VM-P09 resolved; VM-P10 field name mismatch newly surfaced via output hint); vector_editor 4→3 / rule_engine 3→2 / sim_framework 4→3; P2 PROOF UPDATED: H-01/J-01..J-04 updated from "Unknown" to inferred type assertions — 52/52 still PASS; SECTIONS A-L 76/76; no parser / no Rust / no emitter / no new OOF codes; proof: `experiments/record_literal_inference_proof/verify_record_literal_inference_p3.rb`; card: `igniter-lang/.agents/work/cards/lang/LANG-RUBY-RECORD-LITERAL-INFERENCE-P3.md`; next: LAB-STDLIB-STRINGLY-CALL-CONTRACT-MIGRATION-P1 / LANG-STRING-TEXT-ALIAS-P1 |
| [LANG-RUBY-RECORD-LITERAL-INFERENCE-P2](LANG-RUBY-RECORD-LITERAL-INFERENCE-P2-implementation-planning-v0.md) | closed / direct implementation — 55+/55 PASS | Implementation planning + direct implementation for annotated compute record literal field validation; KEY DECISION: temporary `@output_type_hints[name]` install with `ensure` cleanup — reuses 100% of existing `infer_record_literal` validation (lines 2891–2924), zero new methods/structures/OOF codes; INSERTION: `when "compute"` arm before `infer_expr` call, +8 lines; GUARDS: `@type_shapes.key?(tn)` (only known record types) + `!@output_type_hints.key?(name)` (preserves same-name output/compute path); LEAK PROOF: `ensure` block removes hint immediately after `infer_expr` returns; PRESERVED: unannotated computes unchanged / output hint path unchanged / P2 bind_type logic unchanged; EDGE CASES: unknown annotation name → no hint (excluded by @type_shapes guard); non-record annotation (Integer, Collection) → no hint; field errors → OOF-TY0 via existing messages + bind_type still uses annotation as fallback; APP IMPACT: zero (all Wave P4 pressures are unannotated — Section J is regression coverage); P3 PROOF: 55+ checks / 11 sections (A source-insertion/B happy-path/C output-boundary/D missing-field/E extra-field/F type-mismatch/G Unknown-permissive/H unannotated-unchanged/I output-hint-regression/J Wave-P4-regression/K scope-closure); ONE FILE: `typechecker.rb`; NO parser / NO Rust / NO new OOF codes / NO app source changes; proof: `experiments/record_literal_inference_proof/verify_record_literal_inference_p2.rb` 52/52 PASS |
| [LANG-RUBY-RECORD-LITERAL-INFERENCE-P1](LANG-RUBY-RECORD-LITERAL-INFERENCE-P1-record-literal-inference-v0.md) | closed / readiness proved — 49/49 PASS | Research + readiness proof for Ruby TC record literal inference from declared type shapes; KEY FINDING: annotated intermediate computes already fixed by LANG-TYPED-COMPUTE-BINDING-P2 (zero additional app impact); real gap = unannotated intermediate computes where no `@output_type_hints` entry exists; DESIGN: structural field-name+type matching fallback in `infer_record_literal` — one method in `typechecker.rb` (~15 lines); algorithm: collect literal field names → find @type_shapes entries with same field name set → filter by type compatibility (Unknown permissive) → if unique match → infer that type; IMPACT: unblocks 15 symbols across 9 apps (DSA-P10/NN-P09/DF-P10/VE-P08/AP-P12/RE-P07/VM-P09/SIM-P12/SIM-P13); OUT OF SCOPE: annotated computes (P2 handles), if-else branch records (SIM-P05/LAB-IF-ELSE-RECORD-LITERAL-TYPING-P1), nested records, array element records; NO new OOF codes / NO parser changes / NO Rust changes / NO app source changes; proof: `experiments/record_literal_inference_proof/verify_record_literal_inference_p1.rb` 49/49 PASS; next: LANG-RUBY-RECORD-LITERAL-INFERENCE-P2 direct implementation (~15 lines typechecker.rb) |
| [LANG-TYPED-COMPUTE-BINDING-P1](LANG-TYPED-COMPUTE-BINDING-P1-typed-compute-binding-v0.md) | authored — readiness proved 33/33 PASS | Typed compute binding annotation (`compute name : Type = expr`) — both parsers already parse `type_annotation`; Ruby TC does NOT use annotation to guide binding (gap confirmed: B-02 — `[]` still binds `Collection[Unknown]` despite annotation); Rust TC already has `check_array_literal_shape` with output type context (confirmed by Wave P3 Rust CLEAN results); D2/D3 protocol proves that annotation-override-on-Unknown allows clean output boundary check; design: `when "compute"` arm reads annotation → if inferred Unknown-bearing → use annotation as bind type (no `infer_expr` signature change); OOF-TY0 on concrete mismatch; `unknown_or_unknown_bearing?` new helper; 26 cascade Unresolved symbol errors across 8/9 apps addressed; empty `compute xs : Collection[T] = []` → bind `Collection[T]`; stringly BOOTSTRAP + EMPTY_CONSTRUCTOR call_contract patterns partially replaced; no Rust changes / no SIR changes / no parser changes / no stdlib changes; proof: `experiments/typed_compute_binding_proof/verify_typed_compute_binding_p1.rb` 33/33 PASS; next: LANG-TYPED-COMPUTE-BINDING-P2 direct Ruby TC implementation (~12–15 lines `typechecker.rb`) |
| LANG-TYPED-COMPUTE-BINDING-P2 | closed / proved — 48/48 PASS | Bounded Ruby TC implementation of typed compute binding annotation; ONE FILE: `typechecker.rb` only; TWO CHANGES: (1) `when "compute"` arm — after `infer_expr` reads `decl["type_annotation"]`; three-way branch: Unknown-bearing inferred → annotation authoritative (no error) / concrete match → inferred used / concrete mismatch → OOF-TY0 emitted + annotation authoritative; `symbol_types[name]` and `typed_decl` use resolved `bind_type`; (2) `unknown_or_unknown_bearing?` helper after `unknown?` — returns true if type is Unknown or any param depth contains Unknown; SECTIONS: A helper/B empty-array/C concrete-match/D mismatch-OOF-TY0/E unannotated-regression/F output-boundary/G app-pressure-patterns/H chain-propagation/I scope-closure/J edge-cases; 26 cascade Unresolved symbol errors across 8 apps cleared (DSA/VE/DT/AP/DF/RE/NN/VM); `compute xs : Collection[T] = []` → `Collection[T]` authoritative, OOF-TY1 gone at output boundary; concrete mismatch → OOF-TY0 with declared + got types; unannotated compute unchanged; no parser / no Rust / no SIR / no stdlib / no app-source changes; proof: `experiments/typed_compute_binding_proof/verify_typed_compute_binding_p2.rb` 48/48 PASS; arc CLOSED |
| [LANG-FORM-VOCABULARY-P2](LANG-FORM-VOCABULARY-P2-implementation-planning-v0.md) | implementation-planning-only — READY FOR NARROW P3 | Plans phased bounded Ruby canon implementation of form vocabulary declaration/import/validation layer; SCOPE DECISION: P3-C phased (declaration + speaks + typechecker validation + SIR + manifest; invocation deferred to P4 — form-trigger/function-call disambiguation requires type-directed post-classify pass not yet in pipeline); parser: `speaks VocPath` + `vocabulary VocPath { trigger -> ContractRef }` both as top-level declarations in parse_top_decl; KEYWORDS adds `speaks`/`vocabulary`; program hash adds `vocabulary_declarations`/`vocabulary_imports`; classifier: propagates both fields, no fragment class change; typechecker: `vocabulary_registry:` + `per_module_speaks:` kwargs (default {} single-file compat); validate_vocabulary_layer pass post-contract-typecheck; OOF-FORM1 (missing anchor) / OOF-FORM3 (ambiguous import, order-independent) / OOF-FORM4 (invalid owner) / OOF-FORM7 (primitive shadow) / OOF-FORM8 (unknown vocabulary) / OOF-FORM9 (duplicate trigger) active in P3; OOF-FORM2/5/6 deferred to P4; MultifileResolver adds build_vocabulary_registry + build_per_module_speaks (same pattern as cross_module_registry); SemanticIR: form_vocabularies top-level field; typed_nodes nil filter for vocabulary; assembler: form_vocabulary_imports manifest field; dependency_edges UNCHANGED; proof matrix 57 checks 8 sections (regression/parser-vocab/parser-speaks/tc-vocab/tc-speaks/SIR/manifest/authority); ~200–280 lines 7 files; parser/typechecker/SemanticIR/assembler/multifile_resolver/compiler_orchestrator all in scope; VM/invocation/call_contract/package/capability closed | governance boundary for named form words as conservative elaboration over typed-ref substrate; `speaks VocabularyPath` at module level (preferred syntax direction; grammar deferred to P2); V-1..V-9 coherence rules (no ambient leakage, ownership rule, no first-wins, import-order independence, typed-ref anchor required, InvocationIntent equality, no_form propagation, fragment class invariant, primitive reservation); TH-1..TH-6 mapped to concrete rules and prior lab proof evidence; OOF-FORM1..OOF-FORM9 namespace reserved; form_vocabularies/form_resolutions/resugaring_trace SIR design shapes defined; Rust orphan lineage classified (form_registry.rs/form_resolver.rs/FormKind×7/F-01..F-06 kept; AccumulatorRef/FormShape inheritance/MultiKeyword-user deferred); 13 required explicit answers all provided; next LANG-FORM-VOCABULARY-PROP-P2 implementation planning; parser/typechecker/SemanticIR/VM/runtime/public syntax/package/capability/call_contract/macro closed |

---

## Stage 2+ — Open Proposals (authored, not yet experiment-PASS)

These proposals were authored in Stage 2 but their experiments have not yet reached PASS.
They remain active intake. Verification requires Architect authorization.

| File | Status | Summary |
|------|--------|---------|
| [PROP-002](PROP-002-contract-composition-algebra-v0.md) | authored-pending-review | Typed port graph algebra: >>, \|\|, branch, over, embed; algebraic laws |
| [PROP-005](PROP-005-bridge-observation-envelope-v0.md) | authored-pending-review | Obs[kind,T] envelope; Identity/Provenance/Policy groups; ObsPacket |
| [PROP-005.1](PROP-005.1-obspacket-patch-lifecycle-verification-v0.md) | authored-pending-review | Patch document: ObsPacket v0.1 lifecycle field, :verification_observation, WF-10/11 |
| [PROP-007](PROP-007-conformance-verification-v0.md) | authored-pending-review | Verification protocol: 5 check suites, trust levels, agent trust decision |
| [PROP-008](PROP-008-tbackend-contract-v0.md) | implemented-proof | TBackend[T]: read, append, replay, snapshot, compact, subscribe; descriptor fixture PASS (Stage 2); live binding gated (Gate 3 closed) |
| [PROP-010](PROP-010-temporal-lifecycle-retention-semantics-v0.md) | authored-pending-review | 6 lifecycle classes, flush semantics, semantic GC roots, downgrade rules |
| [PROP-016](PROP-016-polymorphism-traits-contract-shapes-v0.md) | authored-pending-review | Generic contracts, traits, contract_shape, monomorphization |
| [PROP-017](PROP-017-schema-evolution-contract-migration-v0.md) | authored-pending-review | SemVer versioning, schema_fingerprint, MigrationDecl, OOF-S1..S5 |

---

## Queued (not yet authored)

PROP-032 is authored and promoted to experiment-pass by proof (assumptions block). New Stage 3+
proposal IDs must consult this queue before claiming a number.

**Queue renumbering (GI-1 resolution, S3-R30-C6-P):** PROP-032 was previously assigned to
`via profile binding`. It is now PROP-032 = `assumptions {}` block. All downstream IDs shift +1.

**PROP-036 lifecycle:** S3-R33-C3-A assigns PROP-036 to `compiler_profile_id`
manifest identity, S3-R34-C5-P authors the proposal, and S3-R35-C3-A accepts it
as proposal-only. It still authorizes no implementation, `.igapp` mutation,
loader/assembler/runtime binding, dispatch migration, or runtime execution
authority.

**PROP-037 lifecycle:** S3-R35-C4-A assigns PROP-037 to external progression and
service liveness semantics; S3-R36-C4-P authors the proposal as proposal-only.
S3-R37-C3-A accepts it as proposal-only, and no parser, TypeChecker, SemanticIR,
RuntimeMachine scheduler, Ledger/TBackend, durable queue, production execution,
ProgressionPack migration, or new fragment class is authorized. Managed local
recursion / loop-class extensions are now authored separately as PROP-039;
PROP-037 remains authoritative for external progression and service liveness.

**PROP-038 lifecycle:** S3-R60-C3-A assigns PROP-038 to
`compiler_profile_contract` after R57-R60 boundary/proof/validator coverage.
S3-R61-C1-P1 authors the proposal, and S3-R61-C3-A accepts it as proposal-only
with implementation held. S3-R62-C3-A authorizes only the first proof-local
implementation under `experiments/compiler_profile_contract_proof/` for
missing-`after` coverage, and S3-R63-C3-A accepts/closes that proof-local gap.
S3-R64-C3-A accepts the Option B library validator extraction design and
authorizes only the next bounded internal proof-parity implementation card.
S3-R65-C3-A accepts and closes that internal validator extraction. It does not
authorize production compiler integration, compile refusal, `.igapp` mutation,
loader/report behavior, CompatibilityReport behavior, dispatch migration,
dynamic pack loading, runtime execution, or production behavior. S3-R66-C3-A
accepts the report-only compiler integration design and authorizes only next
bounded Candidate A implementation: internal provider on `CompilerOrchestrator`
plus in-memory `CompilationReport` field, report-only and never refusal. Public
API/CLI widening, persisted success reports, sidecars, `.igapp`, loader/report,
CompatibilityReport, runtime, and production remain closed. S3-R67-C3-A accepts
and closes that bounded Candidate A implementation with 5 cases / 20 checks PASS.
Public API/CLI widening, `CompilerResult` changes, persisted success reports,
sidecars, `.igapp` mutation beyond proof-local output generation, loader/report,
CompatibilityReport, runtime, Gate 3, compile refusal, and production remain
closed. S3-R68-C3-A accepts the hybrid `contract_digest` validation policy
design: current validator behavior remains `prop038_24_plus` and report-only,
with no `contract_digest` check added now; future validation must pass
shape-only proof before recompute-match proof. Only the proof-local
`prop038-contract-digest-shape-policy-proof-v0` route opens next; implementation,
compile refusal, public surfaces, persisted reports, loader/report,
CompatibilityReport, runtime, Gate 3 widening, and production remain closed.
S3-R69-C3-A accepts the proof-local shape-policy proof with 8 cases / 19 checks
PASS. The diagnostic candidates
`compiler_profile_contract.contract_digest_invalid` and
`compiler_profile_contract.contract_digest_policy_unsupported` are stable enough
for future design/proof work only, not live implementation. Only the proof-local
`prop038-contract-digest-recompute-match-proof-v0` route opens next; live
validator/compiler implementation, compile refusal, public surfaces,
loader/report, CompatibilityReport, runtime, Gate 3 widening, and production
remain closed.
S3-R70-C3-A accepts the proof-local recompute-match proof with 14 cases / 15
checks PASS. Canonicalization material is stable enough for future design/proof,
and the full four-code `contract_digest_*` candidate set is proof-covered across
R69/R70. Only the proof-local
`prop038-contract-digest-report-only-integration-proof-v0` route opens next.
PROP-038 errata and live validator/compiler implementation remain held until
that integration proof is accepted and a separate Architect decision opens them.
S3-R71-C3-A accepts the proof-local report-only integration proof with 12 cases /
21 checks PASS. The three-phase digest proof chain is complete for design
purposes, and the four-code `contract_digest_*` vocabulary is stable enough for
PROP-038 errata/design text. S3-R72-C3-A accepts the errata/design text; the
four-code vocabulary is now canon as PROP-038 design vocabulary. Only the
`prop038-contract-digest-live-implementation-design-v0` route opens next, and it
is design-only planning. S3-R73-C4-A accepts the design and authorizes only one
bounded internal validator implementation card next:
`prop038-contract-digest-live-validator-implementation-v0`. S3-R74-C3-A accepts
that bounded implementation only inside `IgniterLang::CompilerProfileContractValidator`.
S3-R75-C3-A accepts `prop038-contract-digest-compile-refusal-preconditions-design-v0`
as precondition design only. No `contract_digest_*` diagnostic is authorized as
compile-refusal behavior. S3-R76-C4-A accepts the strict-mode/refusal trigger
design and authorizes only `prop038-strict-mode-refusal-trigger-proof-local-v0`
as the next bounded proof-local experiment. S3-R77-C3-A accepts and closes that
proof-local experiment with 12 cases / 15 checks PASS; only
`contract_digest_mismatch` maps to proof-local `would_refuse`, while live
`refused` behavior remains absent. S3-R78-C4-A accepts the live-refusal boundary
design and keeps implementation held. S3-R79-C4-A accepts the internal
orchestrator strict-source/status design and keeps implementation held.
S3-R80-C4-A accepts the strict-refusal result-shape/non-persisting path design
and keeps implementation held. S3-R81-C3-A accepts the proof-local
strict-refusal result-shape experiment with 3 cases / 44 checks PASS and keeps
implementation held. S3-R82-C4-A accepts the live implementation scope review
and keeps implementation held. S3-R83-C1-A authorizes and S3-R83-C2-I lands the
bounded internal-only strict-refusal live implementation; S3-R83-C3-X pressure
passes, and S3-R84-C1-A accepts the slice as the live internal foundation. S3-R85
accepts canon sync and regression/canon map, and S3-R86 accepts Ch5/Ch7/language-spec
sync for that foundation.
Compiler/orchestrator integration, live compile refusal, public surfaces,
`CompilerResult`, persisted reports/sidecars, loader/report,
CompatibilityReport, runtime, Gate 3 widening, and production remain closed.
S3-R251-C1-A authorizes bounded proposal authoring for managed local recursion
and loop-class extensions. S3-R251-C2-I authors PROP-039 as proposal-only text.
PROP-039 does not authorize parser, TypeChecker, SemanticIR, runtime, API, CLI,
package, `igc run`, `.igapp`, `.igbin`, compiler passport, RuntimeSmoke, public
runtime, Reference Runtime, stable API, production, Spark, release, public demo,
public performance, official/reference status, alternative certification,
portability, or lab-canon authority.

Previously queued IDs PROP-033, PROP-034, and PROP-035 are now authored and
experiment-pass in the Stage 3 active table above. PROP-040 owns profile
declarations / authority resolution. Do not reuse those IDs for new proposals.

| ID | Title | Depends On | Stage | Priority |
|----|-------|------------|-------|----------|
| TBD | Broad Effect Surface field/accountability enforcement beyond PROP-035 `capability`/`effect_binding` | PROP-031, PROP-035, PROP-040 | 3+ | medium |
| TBD | Prior queued ideas need renumbering/requeue | — | 3+ | medium |

---

## Deferred Gaps Register

```
Stage 1 deferred gap:
  production_compiler_assembly  → RESOLVED in Stage 2 (PROP-027 + S2-R13 compiler package PASS)

Stage 2 deferred gaps (carried to Stage 3 — see current-status.md §Deferred Gaps):
  production_tbackend_adapter_binding  — Gate 3 closed; live Ledger reads not yet authorized
  olap_distributed_execution           — OLAP scatter/gather, rollup: not yet authorized
  invariant_persistence                — runtime violation observation persistence: open (S3 Runtime lane)
  deferred_invariant_oofs             — OOF-I1 (@bitemporal), OOF-I3 (~T), OOF-I5: deferred
  gem_release_readiness                — publish not yet attempted; release gate PASS (S3-R3-C4)
```

---

## Proposal Lifecycle

```text
draft -> authored-pending-review -> accepted / conditional-accepted
                                      |
                                      v
                              implemented-proof -> experiment-pass
                                      |
                                      v
                                  deferred
```

### Lifecycle Labels

Use these labels in the proposal index `Status` column. Track status is a separate
namespace: `Track: done` means the assigned card delivered its artifact; it does
not mean the proposal is accepted, implemented, or experiment-pass.

| Label | Meaning | Authority / evidence |
|-------|---------|----------------------|
| `draft` | Working text, scope packet, or proposal sketch exists, but no formal proposal file is indexed as the review target | Track evidence or discussion routing |
| `authored-pending-review` | Formal proposal file exists and is indexed; governance has not accepted or rejected it | Proposal file + track handoff |
| `accepted` | Governance accepted the proposal scope | Architect / Meta close decision; implementation may still require a separate card |
| `conditional-accepted` | Governance accepts direction with named blockers, exclusions, or required edits | Gate / decision record with explicit conditions |
| `implemented-proof` | Authorized implementation/proof landed for part or all of the proposal, but full experiment-pass or closure is still open | Track proof, golden, or matrix evidence |
| `experiment-pass` | Verification matrix for the accepted proposal scope passed; the proposal can be closed/frozen by the owning governance flow | PASS evidence + proposal/track reference |
| `deferred` | Proposal or sub-scope is intentionally postponed; not active implementation work | Stage close decision, gate, or explicit routing note |

Historical aliases:

- `proposal` in older docs means `authored-pending-review`.
- `implementation-partial` means `implemented-proof`.
- `closed` is a stage/archive map state; the proposal lifecycle label should name
  the evidence state (`accepted` or `experiment-pass`) instead.
- `patch` is a document type, not a lifecycle status.

Stage 1 PROPs: see `accepted/` — frozen read-only.
Stage 2 closed PROPs: in `proposals/` with lifecycle label `accepted`.
New Stage 3 proposal IDs must consult the queued table above. PROP-033 through
PROP-035 are reserved there; PROP-036 is accepted proposal-only for compiler
profile manifest identity; PROP-037 is accepted proposal-only for external
progression and service liveness semantics; PROP-038 is accepted proposal-only
for `compiler_profile_contract` with first proof-local experiment implementation
accepted/closed, bounded internal library validator extraction accepted/closed,
bounded report-only internal annotation accepted/closed, and hybrid
`contract_digest` policy design accepted; proof-local shape-policy proof accepted
and recompute-match proof accepted; report-only integration proof accepted with
PROP-038 errata/design text accepted; live validator implementation design
accepted; bounded live validator implementation accepted; compile-refusal
preconditions design accepted; strict-mode/refusal trigger design accepted;
strict-mode refusal trigger proof-local experiment accepted/closed;
live-refusal boundary design accepted with implementation held; internal
orchestrator strict-source/status design accepted with implementation held; and
strict-refusal result-shape/non-persisting path design accepted with
implementation held; proof-local strict-refusal result-shape experiment accepted
with implementation held; live implementation scope review accepted with
implementation held; bounded internal-only strict-refusal live implementation
authorized and landed; live internal foundation accepted; canon sync accepted;
and R86 Ch5/Ch7/language-spec sync accepted. Spark CRM is an active
applied-pressure source only, not PROP-038 authority.
PROP-039 is authored-pending-review for managed local recursion and loop-class
extensions. It remains proposal-only and grants no implementation, runtime,
public, reference, stable, release, certification, or portability authority.
