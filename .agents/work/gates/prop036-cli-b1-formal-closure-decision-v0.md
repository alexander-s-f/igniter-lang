# PROP-036 CLI B1 Formal Closure Decision v0

Card: S3-R49-C1-A
Agent: [Architect Supervisor / Codex]
Role: architect-supervisor
Track: prop036-cli-b1-formal-closure-decision-v0
Route: UPDATE
Status: approved-b1-formally-closed-implementation-held
Date: 2026-05-15

---

## Decision

`PROP036-CLI-B1` is formally closed.

The R48 standalone artifact proof satisfies the R46 closure criteria and the
R47 B1 validation-chain precision addendum. The remaining CLI implementation
blockers stay open, and CLI implementation remains held.

This decision does not authorize CLI implementation.

---

## Evidence Read

- `igniter-lang/docs/tracks/prop036-cli-b1-standalone-artifact-proof-v0.md`
- `igniter-lang/docs/discussions/prop036-cli-b1-standalone-artifact-pressure-v0.md`
- `igniter-lang/docs/tracks/stage3-round48-status-curation-v0.md`
- `igniter-lang/docs/gates/prop036-b7-b8-docs-and-criteria-precision-review-v0.md`
- `igniter-lang/docs/gates/prop036-cli-blocker-closure-criteria-decision-v0.md`
- `igniter-lang/experiments/minimal_compiler_profile_finalization_proof/out/minimal_compiler_profile_finalization_summary.json`
- `igniter-lang/experiments/minimal_compiler_profile_finalization_proof/out/compiler_profile_source.stage3_proof.json`

---

## Accepted B1 Evidence

Stable artifact path:

```text
igniter-lang/experiments/minimal_compiler_profile_finalization_proof/out/compiler_profile_source.stage3_proof.json
```

Artifact shape:

```text
artifact.kind=compiler_profile_id_source
artifact.status=finalized
artifact.wrapper=false
slot_count=12
runtime_authority_granted=false
dispatch_migration_authorized=false
```

Required summary fields:

```text
standalone_artifact_path="igniter-lang/experiments/minimal_compiler_profile_finalization_proof/out/compiler_profile_source.stage3_proof.json"
standalone_artifact_exists=true
standalone_artifact_valid=true
standalone_artifact_validation_path="finalization_and_assembler_source_contract"
standalone_artifact_exact_forbidden_token_hits=0
```

Validation-chain path:

```text
finalization_and_assembler_source_contract
```

This satisfies R47 C3-A Amendment 1: `standalone_artifact_valid: true` is not a
JSON-only, field-presence-only, or top-level-shape-only check. It is tied to the
same compiler-profile-source validation path used by the finalization proof and
assembler source contract.

Exact forbidden-token scan:

```text
forbidden_hits=0
```

The scan checks exact JSON keys/scalars against the forbidden token list from
S3-R46-C4-A.

Proof matrix result:

```text
MinimalCompilerProfileFinalizationProof: PASS 27/27
Assembler neighbor regression: PASS 19/19
```

Pressure verdict:

```text
S3-R48-C2-X: proceed
Blockers: none
NB-1: formal B1 closure awaited Architect gate acceptance
```

This decision resolves that non-blocking governance note.

---

## Formal B1 Status

`PROP036-CLI-B1` status:

```text
closed
```

Closure basis:

- proof-owned standalone artifact exists at the stable R46/R47 path;
- artifact top-level JSON value is the source object itself;
- artifact is generated by the named finalization proof command;
- artifact validates through the accepted source-validation chain;
- proof summary records all required fields;
- exact forbidden-token hits are `0`;
- pressure review independently verifies the evidence and reports no blockers.

---

## Remaining Open CLI Blockers

The remaining open blockers are:

```text
PROP036-CLI-B3
PROP036-CLI-B4
PROP036-CLI-B5
PROP036-CLI-B6
PROP036-CLI-B9
```

Already formally closed before or by this decision:

```text
PROP036-CLI-B1
PROP036-CLI-B7
PROP036-CLI-B8
```

`PROP036-CLI-B2` remains satisfied by the existing design route, but this
decision does not re-open or re-authorize it.

---

## Non-Authorization

This decision does not authorize:

- CLI implementation;
- CLI flags;
- path loading;
- JSON parsing in CLI;
- profile finalization, discovery, inference, or defaulting in CLI/API;
- loader/report implementation;
- CompatibilityReport compiler-profile section;
- `.igapp` golden migration;
- `.ilk`;
- CompilationReceipt links;
- signing;
- compiler dispatch migration;
- RuntimeMachine binding;
- Gate 3 widening;
- Ledger/TBackend;
- BiHistory;
- stream/OLAP production executors;
- production cache;
- production behavior.

---

## Next Allowed Boundary

The next pressure card may review this decision.

After pressure review, the next implementation-authorization question may focus
on the remaining `PROP036-CLI-B3/B4/B5/B6/B9` package. Any such card must still
explicitly authorize implementation before touching CLI code, path loading, or
JSON parsing.
